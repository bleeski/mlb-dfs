# Gate pin moved: 1659 -> 1675 (R292 + R300(b))

For ARCHIVE. The Quick Card's copy of the gate line needs updating; this session
does not edit the ledger.

New line:

    PASS  v2.26.0  27 modules  1675 tests

Per-suite moves, `EXPECTED_SUITE_COUNTS` in `tools/audit.py`:

| suite | was | now | state |
|---|---|---|---|
| tests.test_core | 1047 | 1047 | unchanged |
| tests.test_showdown | 173 | 173 | unchanged |
| tests.test_upload_integrity | 332 | **348** | grew |
| tests.test_golden_replay | 9 | 9 | unchanged |
| tests.test_paste_lineups | 98 | 98 | unchanged |

The sixteen new tests pin the repair path and its clocks: eleven on
`tools/repair_entry.py` (the `--out` artifact round-tripping through the loader
that wrote it and clearing the preflight, a candidate whose game is underway
being refused while an `unobserved` one still falls through to the feed, the
observed crosswalk being handed the salary path rather than parsed rows, one
naive `--as-of` reading identically in all three sibling tools, bare `HH:MM` and
Zulu stamps, and `--dead-from-feed`), three on `tools/verify_export.py` (a
started player being a hard failure and not a warning when no parent resolves,
the same file passing before first pitch, and a legal late swap still passing
with a parent), and two on preflight's real `datetime.now` branch, which R300(b)
found had no test asserting its return code.

Two existing tests in `tests/test_upload_integrity.py` were adjusted, neither
weakened, and the count above already accounts for them: `run_verify` pins the
fixture clock the way `run_preflight` has since R287, and
`test_the_feed_is_auto_resolved_from_beside_the_salary_file` passes `--parent`
because its subject is feed resolution at a clock where AAA has locked.

There is already a `2026-09-02_DEV_gate-pin-1659.md` fragment in this inbox
awaiting merge. This one supersedes its number, not its per-suite history; merge
both in order.
