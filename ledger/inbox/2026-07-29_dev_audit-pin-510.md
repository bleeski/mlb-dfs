# Fragment: Quick Card audit line, 437 -> 510

Session: DEV, 2026-07-29 evening, false-PASS cluster (R29). For: ARCHIVE, whose
path `ledger/` is. One edit, in place, to section 0 item 1.

## The edit

Replace both count references in the Quick Card's macro line:

- expected audit line: `PASS  v2.26.0  23 modules  437 tests`
  becomes            `PASS  v2.26.0  24 modules  510 tests`
- the suite-by-suite sum: "the counts sum to 437 (330 + 98 + 9)"
  becomes                 "the counts sum to 510 (359 + 49 + 93 + 9)"

Note the module count moved too, 23 to 24: `mlb_engine/repo_env.py` is new. The
module count comes off the filesystem and is not a pin, but the Quick Card quotes
the whole line, so leaving 23 there makes the quoted line false.

The suite breakdown is now four numbers rather than three because
`test_showdown` and `test_upload_integrity` no longer fit in one 45s sandbox call
together. Suite-by-suite, measured this session with scipy present:

    python -m unittest tests.test_core                                359  (25s)
    python -m unittest tests.test_showdown                             49  (3s)
    python -m unittest tests.test_upload_integrity                     93  (11s)
    python -m unittest tests.test_golden_replay                         9  (17s)
    python tools/audit.py --terse            # pins and inventory, no tests

## Why the count moved

R29, the false-PASS cluster: 73 new tests across five commits. Nothing about what
the audit checks changed, and no existing test was deleted or weakened. Two
existing R25 tests in `test_core` were rewritten to pass postures instead of a
flat controls dict, because `resolve_swap_controls`' first argument changed; they
assert the same two facts they did before.

Docs already re-pinned in the same commits: `tools/audit.py`
(`EXPECTED_TEST_COUNT`), `CLAUDE.md` session-start item 2, and
`skills/generate-lineups/SKILL.md`. This fragment is the fourth quoted copy, and
the ledger is ARCHIVE's to write.

## Also worth a Quick Card line, ARCHIVE's call whether it earns one

`tools/claim.py check` currently reports `slate_2026-07-29_sealad_sd` as HELD and
STALE. It is neither: its owner wrote the `RELEASED` marker four minutes after
taking it, but `_is_held` prefers `owner.json`'s null `released_utc` over the
marker, and `_is_stale` compares the claim name's date against the UTC date, which
after 8 PM ET is already tomorrow. So any evening slate flags its own same-night
claims STALE, and a claim released the way CLAUDE.md defines release reads held.
Filed as R31 in `docs/2026-07-27_backlog_v2.md`. Nothing was released or deleted:
the claim was not this session's to release.
