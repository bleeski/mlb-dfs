# Gate pin moved: 1645 -> 1659 (R291)

DEV, 2026-09-02 ET. For ARCHIVE to merge into the ledger Quick Card, item 1.
The Quick Card is ARCHIVE's write set, so this is a fragment rather than an
edit.

The session-start line is now:

    PASS  v2.26.0  27 modules  1659 tests

Fourteen new tests, all in the two suites R291 touched:

- `tests.test_core` 1040 -> 1047. Seven new; the R289 acceptance test was
  REWRITTEN in place rather than added to, so it contributes none of the seven.
- `tests.test_showdown` 166 -> 173. Seven new.
- `tests.test_upload_integrity` 332, `tests.test_golden_replay` 9,
  `tests.test_paste_lineups` 98, all unchanged.

`EXPECTED_SUITE_COUNTS` in `tools/audit.py` carries the per-suite pins and the
total is derived from them; `CLAUDE.md`, `skills/generate-lineups/SKILL.md` and
`NEXT_SESSION_PROMPT.md` quote the line and moved in the same commit.

Note for whoever merges this: the Quick Card's copy of this line read
`1386 tests` as of the 2026-09-02 ed10 review, four moves stale. That staleness
is R301's, not this fragment's, but the merge is the cheap moment to fix it.

Golden replay: 9 tests, unmoved. Its fixtures carry no `Excluded` column.
