# BUILD 2026-09-04 slate 2140_3g (late, 3 games: NYY@SD, ATH@SEA, WSH@LAD)

- Entries: 17 Classic across 10 contest IDs (4 Dime Time, 5 NFL-Millionaire satellite,
  2 Postseason-Opener satellite, 1 mini-MAX, 1 Solo Shot, 4 Quarter Jukebox).
- run_id 20260905T010426Z_9762156e; certified (workflow_valid, selection_certified,
  allocation_certified all true). preflight_upload PASS, 17 entries, all hard checks clean.
- Lineup source: DK Starting column covered all 6 sides 1-9 complete -> zero-fetch path,
  no paste, no API call. F4 platoon component therefore neutral (f4_platoon_applied 0):
  DK ships no handedness.
- Probable SP: DK marked Max Fried "PO" and Will Warren "PLR"; the engine parses neither,
  so NYY had no probable. Fried declared via --declare-pitcher 44030424 after confirming
  from baseball-reference previews and Pinstripe Alley series preview. Warren left out of
  the pool as a projected long reliever (pool blocker overridden with --ignore-pool-blockers
  + --assume-gates lineup_gate_passed on that same evidence).
- Odds: the-odds-api unreachable as always; built an odds packet from a paste.
  WSH@LAD priced by draftkings (+233/-292, 8.5) and fanduel (+260/-320, 8.5);
  NYY@SD and ATH@SEA priced from Polymarket probabilities converted to American
  (NYY -113/SD +108 tot 7.5; ATH +194/SEA -203 tot 7.5). f1_games_priced 3 of 3.
  Implied team totals: LAD 4.78, SEA 4.14, NYY 3.81, WSH 3.72, SD 3.69, ATH 3.36.
- Controls: supervisor raised max_shared_players to 8 (engine-named structural floor).
  The joint MILP was then proven infeasible with the generic-interaction message and no
  single named control, so R157 applied: sanity check with all three exposure caps at 1.0
  certified (realised max player exposure 0.82), then delivered at
  max_pitcher_exposure_pct 0.50 / max_player_exposure_pct 0.65 /
  max_primary_stack_exposure_pct 0.45. Posture defaults were 0.43 / 0.40 / 0.35.
- Anti-correlation: --max-opposing-hitters-per-sp 1 (R288), engine default is 0.
  16 of 17 entries carry at least one; all DK-legal, preflight WARNs only.
- QA leverage read: every contest chalk-positive against the prior's field mean,
  +29.6 pp (Solo Shot) to +55.8 pp (NFL satellite). Not acted on: the prior is
  uncalibrated and its scale does not meet ledger 3.17's. Filed as the open question.

## Post-delivery enrichment hunt (T-10, not applied)
- mlb.com/starting-lineups had all six sides posted with handedness. Cross-checked
  every slot against DK's Starting column: SIX FOR SIX identical, no disagreements,
  no scratches. The paste's only new information was bat_side, i.e. the F4 platoon
  component the zero-fetch path leaves neutral.
- Built the feed (tools/lineups_from_paste.py, 60 of 60 names resolved, all six
  sides confirmed) at data/slates/2026-09-04/lineups_feed_2140_3g.json. It carries
  a KNOWN BAD ROW: two players named Max Muncy on this slate (ATH 44030645,
  LAD 44030406) and the paste's ATH slot resolved to the LAD row. --resolve cannot
  separate them because both DK full names are identical. WARN only, not a blocker.
  Handedness for LAD Max Muncy (0.53 exposure) would have been wrong.
- Did not rebuild. Two reasons, in order: 9 minutes to lock against an ~80s build
  cycle plus preflight, and a feed with a wrong row on a 53%-exposure hitter.
- Ownership projections: LineStar and FantasyTeamAdvice both login-gated. No free
  slate-level ownership source found. The uncalibrated in-repo prior stays the only
  ownership input, and --leverage stayed off.
- DEV items this surfaced: (1) lineups_from_paste cannot disambiguate two players
  sharing a full name across teams; --resolve keys on the DK name, so the team-scoped
  case has no escape hatch. (2) build_slate does not parse DK Starting values "PO"
  and "PLR", which cost this build a refusal cycle on NYY having no probable.

## CORRECTION to the section above (checked 2026-09-05 after the slate)
The "KNOWN BAD ROW" claim about the Max Muncy collision was WRONG, and the
error was mine, not the tool's. Verified against the written feed:
ATH order 7 -> dk_player_id 44030645, bat_side R; LAD order 5 -> 44030406,
bat_side L. Both DK ids correct, both handedness values correct.
`lineups_from_paste.py` resolved the collision properly; only its
team-disagreement WARNING is a false positive, because that check re-derives
the team from a name-keyed dict after the match was already team-scoped
(paste_lineups.py:803). Filed as
`docs/backlog_inbox/2026-09-04_BUILD_paste-team-warn-false-positive-on-shared-name.md`.

So the honest reason the F4-platoon rebuild did not happen is the clock alone:
the enrichment pass began at T-34 and the session did not read a clock again
until T-9. SKILL.md's rule -- print `TZ=America/New_York date` in the SAME bash
call as every build -- was honoured for every build call and not for the ~24
minutes of web research between them. Twenty-five usable minutes were spent
without a clock reading. That is the same failure the rule already documents,
arriving through a door the rule does not name.
