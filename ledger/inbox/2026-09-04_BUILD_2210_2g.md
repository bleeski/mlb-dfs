# 2026-09-04 slate 2210_2g (WSH@LAD, ATH@SEA) -- BUILD delivery record

- run_id: 20260905T012259Z_f40519d5
- delivered: outputs/2026-09-04/DKEntries_2210_2g_UPLOAD.csv
- sha256: bffea34695c205dc36b8b24f84168dbd7fa48ee76082e357af959068bf52c77f
- gates: workflow_valid / selection_certified / allocation_certified all PASS (upload-ready, Classic)
- entries: 7 across 4 contests (194991402 large_gpp x2, 194991418 wta_satellite x2,
  194992583 small_gpp x1, 194992584 large_gpp x2). Postures operator_supplied;
  Solo Shot forced to small_gpp per the archetype row's own operator note.
- feed: dk_salary_starting, 4/4 sides complete 1-9 in the DK Starting column.
  Zero API calls, zero paste. DK ships no handedness, so F4's platoon component
  had no hand data for any side on the zero-fetch path.
- odds: PASTE, covers.com opening line, unattributed book, labeled covers_open.
  2/2 games priced. F1 live, league mean implied total 3.75.
  api.the-odds-api.com is proxy-gated from this mount as usual.

## Controls, and why they moved
Default build refused: `max_primary_stack_exposure_pct: 3 distinct primary
stacks x cap 2 = 6 < 7 entries`, refusal_class badly_shaped.

1. `--max-opposing-hitters-per-sp 5` (engine default 0). R288 delegated control.
   On a 2-game/4-team slate the convention bans most of the legal space: with
   2 SPs rostered, 2 of the 4 teams are always "opposed", so at 0 only 3 teams
   could ever be a primary stack. Raising it restored the 4th and cleared the
   single-control bind. Every entry carries 1-6 opposing hitters; DK-legal,
   preflight WARNs and passes.
2. `max_primary_stack_exposure_pct` 0.35 -> 0.45, `max_sp_pair_repetition`
   2 -> 3, `max_shared_players` 8 -> 7 (TIGHTER). Laddered from tight; this was
   the first certifying set. R157 sanity check ran first: all three exposure
   caps at 1.0 certified, so the bank could certify at all.
   Pitcher/player exposure left at the posture default 0.5714.

## Findings carried into delivery
- 6 distinct lineups across 7 entries. The repeat is 5241340191 / 5241332360
  (different contests, so DK-legal and free). Tried 0.30 and 0.60 primary-stack
  caps and max_shared_players 7; 6 distinct was the ceiling in every direction,
  so it is a property of an 84-candidate bank on a 2-game slate, not a control.
- qa_portfolio finding: ATH holds 2/7 primary stacks at implied total 3.11,
  slate's bottom third. Those are the same two entries in the degraded tail
  ($4,500 salary left, proxy 29.7% below median). Not a chosen leverage play --
  the allocator filled them from the cheap-SP end of a thin bank. No salary-floor
  control exists to target it (checked the controls vocabulary).
- Washout binds on WSH@LAD at 7/7 entries; 2 games leaves no way to avoid that.
- Chalk-sum positive in all four contests (+28.3 to +48.3 pp vs field mean).
  Directionally with ledger 3.17's OBSERVED winner-minus-field medians for
  satellites (+4.9) and solo shots (+9.5); the two scales do not meet until the
  prior is fit.
- preflight WARNs about a 4.5h-old lineups_feed.json and "4 teams have no
  confirmed lineup": that feed is the earlier 1810_3g draftgroup's. This build
  never read it. DK's own Starting column covered all four sides of THIS
  draftgroup. Spurious for 2210_2g.

## Worth filing as DEV work
`preflight_upload.py` / `verify_export.py` cross-check against whatever
`lineups_feed.json` is on disk without checking that it belongs to the
draftgroup being verified. On a night with three draftgroups (1810_3g, 2140_3g,
2210_2g) that produces a confident, wholly spurious "28 rostered players absent
from a posted lineup" block on a build that used the DK zero-fetch path. It
reads exactly like a real staleness problem under a lock clock.

---

## SUPERSEDED by the enrichment pass (same session, 21:40 ET)

Ben asked whether outside data would change the build. It did. Delivery is now:

- run_id: 20260905T013328Z_7aea8b72
- sha256: a5ff792a67f9a3499210de3616ed027474d906570e69b5557b012627e2f8003f
- same path: outputs/2026-09-04/DKEntries_2210_2g_UPLOAD.csv
- gates all PASS; preflight and verify_export both exit 0 at 21:40 ET.
- The earlier delivery (bffea34695c2, run 20260905T012259Z_f40519d5) is SUPERSEDED.

### What changed and why
`f4_platoon_applied` was **0** on the first build. The DK zero-fetch path (R143)
gives a complete 1-9 but no handedness, so F4 scored all 36 hitters on opposing-SP
quality alone and the platoon component contributed nothing.

Closed it: WebFetch REACHES statsapi.mlb.com and mlb.com/starting-lineups from the
CONTAINER, though device_bash and the container's own urllib are both still
proxy-gated (403). That is a usable new route and worth remembering. Pasted
mlb.com/starting-lineups through `tools/lineups_from_paste.py` (R32 path) ->
`data/slates/2026-09-04/lineups_feed_2210_2g.json`, 40/40 names resolved, all four
sides confirmed. `f4_platoon_applied` 0 -> 36.

mlb.com's batting orders matched DK's `Starting` column on all four sides, so
there were no `dk_batting_order.disagreements` to arbitrate.

### Two paste gotchas on this slate, both worth keeping
- `E Hernandez` (mlb.com renders "Enrique Hernández") does NOT match DK's
  "Kike Hernandez". Reported as `NOT IN DK POOL`, which is the tool being
  literally correct and materially misleading: DK DOES list him (44033028,
  Starting 9). It dropped LAD to PARTIAL 8/9 and routed the slate's best stack
  through the TBD path. Fixed with `--resolve "E Hernandez=Kike Hernandez"`.
  Worth an alias row: Kike/Enrique is a recurring DK-vs-mlb.com name split.
- Two Max Muncys on one slate (44033030 LAD, 44033190 ATH). The tool WARNs
  "paste puts 'Max Muncy' on ATH and the salary file puts him on LAD" -- that
  warning is a false alarm at the name level. Checked the written feed: LAD slot 5
  -> 44033030 (bats L), ATH slot 7 -> 44033190 (bats R). Both correct. Do not
  "fix" this warning.

### F4 direction verified against the frame, not assumed
Opposing SPs: LAD faces Kent (L), WSH faces Snell (L), SEA faces Morris (R),
ATH faces Gilbert (R). Read off runs/<rid>/final/projections.csv:
  LAD  Betts (R) 1.080  Teoscar (R) 1.080  Tucker (L) 0.985   <- Kent is weak, level high
  WSH  Crews (R) 0.927  Wood (L)    0.850                     <- Snell is strong, level low
  SEA  Naylor (L) 1.035 Montes (L)  1.035  Raleigh (S) 1.015
  ATH  McNeil (L) 0.936 Bolte (R)   0.891                     <- Gilbert is strong, level low
Quality sets the team level, handedness differentiates within it. Correct in all
four sides. LAD grades best on the slate: highest implied total (4.54) AND the
weakest opposing starter (Kent, 5.59 ERA) AND righty-heavy against his left hand.

### Cost of the change
apex 876.23 -> 883.84, salary-left worst $4,500 -> $4,300, and the washout
histogram evened out (both games now 3-5 bats per entry; the old file had an
8-bat entry on WSH@LAD). Distinct lineups fell 6 -> 5. Swept 0.35/0.45/0.60
primary stack, sp_pair_repetition 2/3/4, shared_players 6/7/8: 5 distinct is the
ceiling with the platoon live and every tighter config REFUSED. Took the
information over the one extra construction; both duplicate pairs sit in
DIFFERENT contests, so within-contest duplicates are still 0.

### Leverage: tested, measured, NOT shipped
`--leverage` needs an archetype named when the prior carries several
(`{"archetype":"large_field_gpp",...}`) -- the refusal names them.
  cap 95 / min_low_owned 1: CERTIFIED, 6 distinct, apex 883.84 -> **688.27 (-22.1%)**
  cap 90 / min_low_owned 2: REFUSED
The skill's reference point is -7.72% at cap 90 on 1605_2g; -22.1% at cap 95 here
is far steeper, because a 2-game slate concentrates the prior over ~40 rosterable
bats. Against that, ledger 3.17's OBSERVED winner-minus-field medians are POSITIVE
for three of this file's four contests (satellites +4.9, solo shots +9.5), i.e.
winners in those archetypes ran chalkier than the field, not lighter. Paying 22%
of the apex proxy to move away from chalk is moving against the archive here.
Recorded as a direction tested with its number, not a recommendation.

### Checked and found NOT worth changing
- Savant reference (expected_stats_batting/pitching): 4 days old, inside tolerance.
- FanGraphs platoon reference: never read on this slate, all four sides confirmed.
- RotoWire: proxy-gated from the device as always; adds nothing over the paste.
- Public projected ownership: dailyfantasyfuel carries this slate but its OWN
  column is empty ("--"). No public DFS ownership projection was found for a
  late-night 2-gamer. The engine's own uncalibrated prior remains the only one.
