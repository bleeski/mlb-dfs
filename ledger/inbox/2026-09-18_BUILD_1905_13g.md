#### BUILD fragment — 2026-09-18 slate 1905_13g (13 games, 22 Classic entries, Claude Code cloud container)

Deterministic review proxies and labeled priors only; never ROI, win rate, cash
rate, or a probability. Delivered file sha256
`63a37038e80a5987b100b978c3b6319e22023e718a84d4713c8fae274c01ef2f`, all three
gates true, `preflight_upload` exit 0.

**R157 re-derivation on this slate, recorded so the next one has a comparison.**
The posture-default joint MILP refused as `badly_shaped`: "no single control is
arithmetically binding against this bank, so the interaction of the active
controls is. Active: max_pitcher_exposure_pct=0.43, max_player_exposure_pct=0.35,
max_primary_stack_exposure_pct=0.35, max_shared_players=6,
max_sp_pair_repetition=1, max_team_exposure_pct=0.55".

- Sanity check with all three exposure caps at 1.0: **certified**, so the bank
  was never the problem. Realized max player exposure **16/22 = 0.727** (Jo Adell,
  CLE), James Wood 14/22, Miguel Vargas 12/22. One 0-for-4 would have taken down
  73% of the entered set, which is why 1.0 is not shippable here and not only as
  a matter of policy.
- Realized at 1.0 on the other two axes: primary stack max **3/22 = 0.136**,
  pitcher max **6/22 = 0.273**. Both sit UNDER their posture defaults (0.35,
  0.43), so neither of those two caps was binding and neither was loosened in the
  delivered build. Only `max_player_exposure_pct` moved.
- Ladder walked: 0.35 refused (proven infeasible) -> **0.45 certified**, realized
  max 9/22 = 0.409, 7 players pinned at the cap, 0 reuse relaxations -> **0.40
  certified and delivered**, realized max 8/22 = 0.364, 85 distinct players, 17
  primary stacks. 0.40 needed the engine's own candidate-reuse cap to step
  (1 -> 2 -> None, `proven_infeasible_with_engine_default_reuse_cap`), but the
  relaxation went UNUSED: delivered `max_signature_repeat` 1, `distinct_lineups`
  22, duplicates within and across contests 0.
- **Why the slate floors here at all.** The bank is concentrated by the
  projection, not by thin search: James Wood sat in 184/264 bank candidates
  (69.7%), Vargas 157/264 (59.5%), Adell 152/264 (57.6%). A cap of 7 needs 15
  candidates free of each of the three simultaneously, and that intersection is
  what was empty. 0.40 on a 13-gamer is therefore a statement about projection
  concentration on three value bats, not about slate size; re-derive rather than
  carrying 0.40 forward.

**Host finding, Claude Code cloud container — the odds path is fully closed.**
Worth a `docs/hosts.md` line if DEV agrees.

- `statsapi.mlb.com` is proxy-gated (403 CONNECT), as are `api.the-odds-api.com`,
  `site.api.espn.com`, `www.espn.com`, `sportsdata.io`, and WebFetch on
  `vegasinsider.com` / `covers.com` (`EGRESS_BLOCKED`). `THE_ODDS_API_KEY`
  resolves from neither the environment nor `REPO/.env` on this host.
- The `DFS_Architect_MCP` server substitutes for the blocked statsapi fetch:
  `get_mlb_lineups` and `get_mlb_probables` both returned real
  `source: "mlb-stats-api"` payloads WITH handedness on hitters and probables,
  which is the one thing the schedule hydrate omits (R190). A feed hand-shaped
  from them in `fetch_lineups_feed`'s structure gave `f4_platoon_applied: 162`
  and `f4_non_neutral: 225`, so F4 was fully live with zero repo network access.
- **`get_vegas_lines` on that same server is NOT usable: it returned
  `source: "stub"`, `confidence: "stub"`, with one fabricated game (BOS @ NYY)
  that is not on this slate.** It must not be fed to `--odds`. So F1 was neutral
  for this build: `f1_games_priced: 0`, `f1_non_neutral: 0`, and the ownership
  prior's `implied_totals` feature INERT. Five of six enrichment factors live;
  F1 is the only dead one, and on a 13-gamer including Coors that is the
  build's main weakness.

**Pool shape.** Feed coverage 26/26 sides; 18 confirmed (15 from DK's own
complete `Starting` 1-9 per R143, plus TOR/WSH/DET from the MCP pull), 8 TBD —
ARI, LAA, LAD, MIA, MIN, NYY, SD, SF, i.e. the four late West-coast games, which
genuinely had not posted at build time. Those filled from
`fangraphs_platoon_lineups.json` at **44.9 days old against a 7-day R27 limit**,
and 43 of 220 delivered roster slots (19.5%) sit in unposted games across ARI,
MIA, MIN, NYY, SD. The preflight names them; a late swap once they post is the
natural follow-up.

**Three teams shipped no rosterable arm.** CLE, PHI and STL each declared a
probable OPENER (Daniel Espino, Tim Mayza, Kyle Leahy, all `Starting=PO`, barred
from P slots), with the bulk arm behind each flagged `PLR` (Joey Cantillo,
Jose Urquidy, Cooper Hjerpe) and therefore outside the pool. The three PO
blockers were overridden as benign and the three PLR ones left as the engine's
default, because confirming a bulk-arm role is a web search and this host has no
web egress. 23 rosterable arms across 13 games is ample for 2 P slots over 22
entries. Note `overridden_gates` came back **null**: `lineup_gate_passed` derived
TRUE once the blockers were overridden, so nothing was asserted against a False
derivation. Hitters from all three teams stayed in the pool throughout.

**Reference staleness at build time**, both past the 14-day limit and used
anyway as designed: `expected_stats_batting.csv` and
`expected_stats_pitching.csv` 19.1 days. `refresh_reference_data.py` fetches
Savant over HTTP and was not attempted here given the egress findings above.

**QA (`qa_portfolio.py`), one adversarial finding, not acted on:** Cade Cavalli
at 5/22 carries ERA 3.12 against xERA 3.83 (+0.71), surface line ahead of
contact quality. Left standing at 23% exposure rather than rebuilding for one
arm. Frontier: washout proxy 36% (binding axis `starting_pitcher`, Dylan Cease
8/22), apex-concentration proxy 14% on the heaviest primary stack across 15
distinct stacks, degraded tail 0/22. `max_opposing_hitters_per_sp` left at the
engine default 0 (R288): on a 13-gamer the constraint barely binds, so the
1940_9g cost does not apply here.
