# ROADMAP | MLB DFS Engine | the single source of truth for engineering work

> **Quick start.** Read `docs/ROADMAP.md`, execute the Session Block on the **NEXT** line, and verify completion using that row's Verification command followed by `python tools/plan_status.py --check` and `python tools/audit.py --run-tests --terse`. On a pass, mark the row `Complete <date>`, advance **NEXT**, append a Progress Ledger row, and write the `CHANGELOG.md` entry in the same commit (`/land`, then `/ship`; `/dev-session` runs all of it).

**NEXT:** Session 12

## How this file works

- **Authority.**
  - This file alone orders the work and records its status.
  - `docs/backlog.md` is the **R-entry register**: the What/Why/Fix body of each R-number named here. Read an entry with `grep -n "^### R<num>\." docs/backlog.md`; never read the file whole.
  - `CHANGELOG.md` records what shipped and why.
  - Nothing else is a backlog. A new item is filed as a register entry AND a row (or row edit) here, in the same commit. Fragments still arrive in `docs/backlog_inbox/`, and only DEV merges them.
- **The linter.** `python tools/plan_status.py --check` fails the gate when:
  - an open register entry (a `### R` heading without `CLOSED`) is named nowhere here;
  - a non-Deferred row names no R-number;
  - a Status is outside the vocabulary;
  - **NEXT** is not a Pending or In Progress row.

  `--print` gives a one-line-per-session view.
- **Status vocabulary:** `Pending`, `In Progress`, `Complete <YYYY-MM-DD>`, `Deferred`.
  - A batched block is Complete only when every item in it is.
  - A partial landing rewrites the row to its remainder, and the ledger records what landed.
- **Packaging.**
  - `Standalone`: one work unit with high blast radius (solver, state machine, certification, orchestration).
  - `Batched`: 2-4 cohesive, lower-risk units on one subsystem. One commit, one CHANGELOG entry per R-number.
- **Sizing.**
  - Every block fits one Claude Code context window without compaction. `docs/hosts.md` has the gate's time on each host.
  - Each row names its breakpoint, the point where a partial landing is still a valid commit.
  - A block that outgrows its window lands the finished part and splits its row.
- **IDs.** `Session NN` is the only live ID, two or three digits (R413: two-digit IDs run out at 99, and 89 is the last free one). `was CC-N` and `old Session N` in Source Origin resolve references written before 2026-09-22; those tables sit verbatim under `# Board history` in the register. 88 is the ARCHIVE block; 90-92 are the Deferred pools.
- **Commands** (every row also requires `LINT`, then `GATE`, then `/land` and `/ship`):
  - `GATE` = `python tools/audit.py --run-tests --terse`. It must print `PASS  v2.26.0  <N> modules  <N> tests` with nothing appended.
  - `LINT` = `python tools/plan_status.py --check`, exit 0.
  - `UT <module.Class ...>` = `PYTHONHASHSEED=0 MLB_DFS_ARTIFACT_ROOT="$(mktemp -d)" python -m unittest tests.<module.Class> ... -v`.
  - `GOLD` = `UT test_golden_replay`, unmoved unless the row says it re-freezes, with the histogram before and after.
  - `PROBE` = `python tools/solver_probe.py --date 2026-06-03` prints FITS. Required when the bank, allocator or optimizer changes.
  - A test class named in a row that does not exist yet is created by that session under that name.
- **Path key:**
  - `BS` = `skills/generate-lineups/scripts/build_slate.py`
  - `EP` = `mlb_engine/pipeline/execution_pipeline.py`
  - `CA` = `mlb_engine/allocate/contest_allocator.py`
  - `DKM` = `mlb_engine/entries/dk_entries_manager.py`
  - `UM` = `mlb_engine/entries/upload_manifest.py`
  - `DR` = `mlb_engine/entries/delivery_record.py`
  - `DG` = `mlb_engine/pipeline/deadline_governor.py`
  - `BSM` = `mlb_engine/pipeline/build_state_manager.py`
  - `PF` = `tools/preflight_upload.py`
  - `VE` = `tools/verify_export.py`
  - `AB` = `tools/autobuild.py`
  - `LS` = `tools/late_swap.py`
  - `LSM` = `mlb_engine/swap/late_swap_manager.py`
  - `LDA` = `mlb_engine/intake/live_data_adapters.py`
  - `SIM` = `mlb_engine/intake/slate_intake_manager.py`
  - `SD` = `mlb_engine/optimize/showdown.py`
  - `SDT` = `mlb_engine/optimize/showdown_theses.py`
  - `BC` = `mlb_engine/optimize/bank_cache.py`
  - `OPT` = `mlb_engine/optimize/optimizer_v3.py`
  - `PB` = `mlb_engine/projections/projection_builder.py`
  - `FM` = `mlb_engine/field/field_miner.py`
  - `QA` = `tools/qa_portfolio.py`
  - Line numbers are at `bdf03e3`; re-grep before editing.

## Operating principles (Ben, 2026-09-22)

1. **Delivery first.** A legally valid, accessible portfolio before the effective deadline outranks optional research, weather and odds lookups, simulation and quality gates. An entry-mapped, validated baseline comes first; everything after it improves it or leaves it alone.
2. **Honest gate separation.** Every check is exactly one of these, and Mixed checks are split into their facts, never disabled wholesale:
   - **V** (submission validity or authority): IDs, salaries, eligibility, slots, entry mapping, template structure, locks, exact delivered bytes. Never faked, never relaxed.
   - **S** (strategic preference): exposure, stacks, overlap, reuse, anti-correlation, ownership. Relaxed autonomously when necessary unless an explicit, current never-relax applies (distinct lineups per contest is one: F-3).
   - **P** (process, model confidence, bookkeeping, research completeness): recorded truthfully, and never alone a reason to withhold an independently V-valid file.

   An S or P failure changes the label to review-grade. It never removes the file. "Upload-ready" keeps CLAUDE.md's meaning: a certified Classic export with all three certifications passing.
3. **One source of truth:** this file, enforced by `LINT`.
4. **Session sizing:** one window, one breakpoint, one objective command.

**Priority rule** (replaces the 2026-09-15 prize-first rule): D (delivery) > V (validity truth) > C (capture transport) > T (throughput) > P (prize) > G (guard/record) > H (hygiene). A row's "Needs" names its hard prerequisites.

**Walls every row keeps** (CLAUDE.md):
- no scripted DK access;
- never trim the legal pool (a fast solve means MILP time or gap limits, never a player cut);
- `scipy.optimize.milp` only;
- sorted sets and `PYTHONHASHSEED=0`;
- `runs/<id>/final/DKEntries.csv` is never rewritten;
- a file already presented to Ben is never silently superseded.

## Facts Ben answered (2026-09-22)

| Fact | Answer | Consequence |
|---|---|---|
| **F-1** Upload allowance before first lock | 5 minutes | Effective deadline = first lock minus 5 minutes (or `--deliver-by` if earlier). The publication reserve (validation, write, handoff) is measured inside that bound. |
| **F-2** Partial DKEntries upload | DK accepts it when unresolved rows are REMOVED; blank rows are not the form | Partial salvage and a partial baseline drop unresolved rows. Preflight validates every remaining row against the template and reports the removed Entry IDs as unresolved coverage, not invalidity. |
| **F-3** One lineup in two entries of one contest | DK allows it; Ben never wants it | Distinct lineups per contest is an operator never-relax control (S, provenance `operator_never_relax`), kept under every deadline. The legality check at DKM L1074-1079 stays, reclassified from platform rule to operator rule. |
| **F-4** Deadline-aware delivery scope | Every build, not only with `--deliver-by` | The effective deadline, the baseline and final-window S/P relaxation are the default. This ends R290's "no delivered byte changes without the flag" promise. |

## Spec triage: `docs/2026-09-22_deadline_delivery_qa.md`

Re-verified at `bdf03e3`; no `.py` change since the audited `5b83165`.

| Element | Verdict | Disposition |
|---|---|---|
| DD-01 no early baseline | ACCEPT, confirmed | Sessions 10-12. There is no projection frame before BS L2458. The baseline uses the unenriched rebuild (`EP._assemble_projection_frame(..., "emergency_proxy", None, None, None, ...)`, the path the BS L2483-2486 ValueError branch already takes), and the probe lineup (BS L2539) becomes candidate #1. |
| DD-02 no single deadline | ACCEPT, confirmed | Sessions 15-17. R98(3)/F18 is elevated from CC-17 ahead of R87. The reserve lives in `repo_env.host_profile` (R349), measured at the maximum entry count, never a literal. The 6.0-minute window (DG L91) survives only as a labelled fallback. |
| DD-03 thin bank bypasses recovery | ACCEPT, confirmed (BS L2689 returns before DG at L2856) | Session 18, which carries all of R285 including NF-1. |
| DD-04 usability coupled to quality | ACCEPT, hints corrected (gates DKM L44-68; `validate_upload_ready_gates` L181; allowlist L197-201) | Sessions 05 and 09. The existing `CLASSIC_GATE_CLASS` (BS L383-425) is the seed, not a parallel table. |
| DD-05 autonomy narrower than authorization | ACCEPT; 14 stop sites plus one silent exit | Session 01 (principles), Sessions 07, 19, 20, 21 (code). |
| DD-06 no partial coverage | ACCEPT; format settled by F-2 | Session 22 (R401). |
| DD-07 governor opens an incomplete set | ACCEPT with a nuance: Showdown's global captain cap IS opened; only `max_cpt_per_contest` stays closed | Sessions 06, 19, 20. |
| DD-08 bookkeeping blocks delivery | ACCEPT. The Classic brief also never carries `manifest_recorded` (EP L5511) | Session 03 (brief), 13 (preflight), 23 (manifest). |
| DD-09 labels disagree | ACCEPT; a live mislabel today (EP L5445/L5684 record `certified`; PF L2937 stamps `upload_ready`) | Session 03, pulled forward. |
| DD-10 record lags its CSV | ACCEPT; at HEAD 4 of 8 records carry `entries: []` and populated rebuild records are likely one build stale | Session 02, first code session. |
| Packages A-F | ACCEPT | A → 05, 06, 13, 09, 03 (R388 a-e). B → 10-12, 15-17. C → 07, 18-22. D → 08, 23. E → 02, 03, 24. F → 25-26, the release gate. Phase D is not production-ready until 25 and 26 pass. |
| Bounded recovery contract (§8) | ACCEPT | Sessions 08 and 19. No universal relaxation order. |
| "Deadline policy must be measured" | ACCEPT | Session 15 measures; Session 24 records per-run margins. |
| Reconcile R98(3)/F18, R290, R125(b)(c), R302, R369/R371 | ACCEPT | Session 15; R401 in Session 22; Session 01 (decision) with 19-21 (build); Session 10 (copy `production/workflow.py`'s pattern; stages A+ stay Deferred); Sessions 02, 03, 24. |
| Non-scipy fallback construction path | REJECT | CLAUDE.md: `scipy.optimize.milp` only. A missing scipy is an environment failure the SessionStart hook repairs, and the T-6 hand-build already validates through preflight. |
| Relax the under-5-of-9 crosswalk wall | REJECT | Guessing IDs is V, and CLAUDE.md reserves the crosswalk refusal for Ben (BS L2340-2362). Unchanged. |
| A labelled default posture keeps a build going | ACCEPT as review-grade only | Contest identity is a no-override gate (EP L5177-5183); a default posture never certifies (Session 21). |
| Duplicate lineups to complete a file | ANSWERED (F-3): never | Kept as never-relax. |
| BUILD sessions run the full gate at start | MODIFY | Session 01: inside T-30 a BUILD session skips `GATE` and the solver probe (engineering health, not file validity). |

Found by this consolidation, not in the audit:
- NF-1: AB L696 reads `solve.bank`; refusals write `bank_exploration` at BS L2997. A rider on R285, Session 18. The 1905_10g fragment confirms that the sliced path misses too.
- NF-2: `REFUSAL_EXIT_NOTES` (BS L6044-6049) swaps exits 3 and 4 against the docstring (L14-18) and AB L662. R396(a), Session 03.
- NF-3: exit 5 has no handler, and running out of `--max-attempts` writes no stop record (AB L145, L752). R396(b), Session 07.

## Master Execution Roadmap

### Phase 0: consolidation

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 00** | Standalone | R385: this file becomes the only order/status authority. <br>• The register header is rewritten, and the queue sections are relocated verbatim to Board history. <br>• R386-R404 are filed. <br>• Six fragments are consumed. <br>• PROGRESS.md is replaced by the `plan_status.py` linter. <br>• The hook, skills, rules, CLAUDE.md, MANIFEST and `.audit` pointer are repointed. <br>• The audit is committed. | Ben 2026-09-22; audit §8 | Process/Model (P) | `docs/ROADMAP.md`, `docs/backlog.md`, `docs/PROGRESS.md` (deleted), `docs/2026-09-22_deadline_delivery_qa.md`, `tools/plan_status.py`, `tests/test_core.py`, `tools/audit.py`, `.claude/hooks/session_start.py`, `.claude/skills/{dev-session,land,ship}/SKILL.md`, `.claude/rules/board.md`, `.claude/agents/dfs-premise.md`, `CLAUDE.md`, `MANIFEST.md`, `.audit/NEXT_IMPLEMENTATION_RUN.md`, `docs/cowork_archival_runbook.md`, `BS` (one comment), `CHANGELOG.md` | `LINT`; `UT test_core.PlanStatusTests test_core.SessionStartInboxTests test_core.RootContractBudgetTests`; the hook prints `**NEXT:** Session 01`. Breakpoint: after the register splice. | Complete 2026-09-22 |

### Phase D: delivery (audit packages A-F)

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 01** | Standalone | R386, the delivery-first contract, principles only. Procedure text lands with the session that builds it. <br>• CLAUDE.md's "Ben's, not the session's" paragraph and T-schedule: S and P failures are the session's to relax or record under deadline; V stays a wall. <br>• Inside T-30 a BUILD session skips `GATE` and the solver probe. <br>• `--accept-downgrade` on an authorized repair is the session's. <br>• F-1..F-4 recorded. <br>• R125(b)(c) decided by Ben's instruction. <br>• V/S/P defined in MLB_Classic.md. <br>Backfill Session 00's ledger SHA first. | DD-05; audit §5; Package A (authority); R125(b)(c) decision | Split Mixed | `CLAUDE.md` (≤200 lines / 18,000 bytes), `MLB_Classic.md`, `skills/generate-lineups/SKILL.md` | `UT test_core.RootContractBudgetTests`; `python -m pytest tests/test_upload_integrity.py -k PreflightContractDocumentation -q` | Complete 2026-09-22 |
| **Session 02** | Batched | R387 + R377 + R403, the delivery record binds the bytes it describes. <br>• (a) Entries come from the provisional bytes on all three writers: UM L423 before L435; BS `run_showdown` L4041 before L4061; LS L919 before L935. DR asserts that the sha256 of the entries source equals `manifest_row.sha256`. <br>• (b) R377: Showdown and late swap pass controls and relaxations; every record takes egress from the session-start reading. The Classic caller (`EP._deliver_mirror`) moved to Session 03, because this session was barred from EP. <br>• (c) R403: refusal records carry the slate tag, `refusal_class`, `errors[:3]` and the failing `feasibility.checks` (`_main_recording_refusals` passes `slate_tag=""`; eleven `untagged_*.json` on 1905_10g). Run-less records are keyed on UTC or a sha256 prefix, not `<tag>_norun.json`, where the 18:23 repair and the 19:05 late swap collided. | DD-10; inbox `2026-09-22_BUILD_delivery-record-reads-dest-before-promote`; R369; R377 (was unslotted); inbox `2026-09-22_BUILD_1905-autobuild-sliced-read-and-untagged-refusals` §2; inbox `1905-late-swap-needs-feed-dk-starting-ignored` §3 | Split Mixed | `DR`, `UM`, `BS`, `LS` | `UT test_upload_integrity.DeliveryRecordBytesTests test_upload_integrity.R96UnrecordedDeliveryTests test_core.RefusalRecordTagTests` (a rebuild's record rosters equal the delivered file's; a refusal record carries a non-empty tag and the error text; two run-less deliveries write two records). Breakpoint: (a) ships alone. | Complete 2026-09-23 |
| **Session 03** | Batched | R388(e) + R298 + R396(a) + R377's remainder, every surface tells the same story. <br>• (a) A governed Classic retry records `review_grade_deadline` in the manifest inside `run_slate` (EP L5445, L5684), so brief, manifest, preflight verdict (PF L2924-2954) and delivery record agree. `LS --accept-downgrade` records review-grade (L925-926). <br>• (b) R298: the three T-5 evidence readers, including the Classic brief's missing `manifest_recorded` and `mirror_error`. `tools/retro.py`'s stamp becomes `manifest_recorded_utc`, not gate-clean (R371 correction). <br>• (c) NF-2: correct `REFUSAL_EXIT_NOTES`. <br>• (d) R377's remainder, from Session 02: `EP._deliver_mirror` passes `controls=result.get("merged_controls")`, so a Classic delivery record carries its controls. <br>The test drives the real `run_slate` → manifest → preflight chain, not the mocked wiring test. | DD-09 (live today); DD-08 brief half; R298 (was CC-38 b1); R371; NF-2 | Process/Model (P) | `EP`, `BS`, `PF`, `LS`, `tools/promote_run.py` (a second Classic delivery caller the premise check found), `tools/retro.py`, `tools/qa_portfolio.py` (R298(c)'s `find_prior_file`; the row named `tools/ownership_pred.py`), `skills/generate-lineups/references/retro.md` | `UT test_core.DeliveryLabelAgreementTests test_core.DeadlineGovernorWiringTests test_core.RetroFactsTests test_upload_integrity.DeliveryRecordBytesTests`. Breakpoint: (a) ships alone. | Complete 2026-09-23 |
| **Session 04** | Batched | R399(a)(e) + R397 + R170, blockers found live. <br>• (a) `BS --help` crashes on "100%" in the `--captain-prior` help (fix: `%%` plus a `format_help()` test). The `--captain-sleeve` help and `brief.captain_sleeve.label` are stale after R382, and the block drops the `selector` report. <br>• (b) `mlb_engine/intake/paste_odds.py` holds the "no priced row" refusal `tools/odds_from_paste.py` prints: exempt postponed, cancelled and suspended games on `--feed` (the pool's classifier) or `--exclude-game`, named `excluded_postponed`. <br>• (c) R170: a missing `--odds` path becomes a live fetch (and `--bundle`'s silent skip, the same class); the preserve-fallback re-mints the tag. | inbox `showdown-1915-small-defects` items 1 and 5; inbox `odds-paste-blocks-on-postponed-game`; R170 (old Session 54) | Process/Model (P) | `BS`, `SDT`, `mlb_engine/intake/paste_odds.py`, `tools/odds_from_paste.py`, `tests/test_core.py`, `tests/test_showdown.py`, `tests/test_paste_lineups.py`, `tools/audit.py` | `python skills/generate-lineups/scripts/build_slate.py --help > /dev/null`; `UT test_core.BuildSlateHelpTests test_core.PreserveTagAndNamedOddsTests test_paste_lineups.OddsPastePostponedTests test_showdown.CaptainSleeveBriefTruthTests` | Complete 2026-09-23 |
| **Session 05** | Standalone | R388(a), one gate taxonomy. <br>• Promote BS `CLASSIC_GATE_CLASS` and DG's by-value copy of the four class strings into `mlb_engine/entries/gate_classes.py`. <br>• Every PRE/POST export gate, `FORBIDDEN_CALLER_ASSERTIONS` (DKM L44-68), `allocation_certified`, `allocation_method` and every `REFUSAL_SITES` key gets V, S, P or MIXED, with MIXED split into facts per audit §3. The existing `authority` field is kept. <br>• Evidence states: `passed`, `failed`, `not_checked`, `assumed`. <br>• BS imports it lazily (it loads without the engine). <br>• An unclassified name raises in the V/S/P lookups; the governor's lookup keeps its READ-IT default. <br>No behavior change. | DD-04; Package A; audit §3 | Split Mixed | `mlb_engine/entries/gate_classes.py` (new), `BS`, `DG`, `DKM`, `MLB_Classic.md`, `tests/test_upload_integrity.py`, `tests/test_core.py`, `tools/audit.py` | `UT test_upload_integrity.GateTaxonomyTests test_core.RefusalClassificationTests` (`test_every_workflow_gate_is_classified` keeps passing); `GOLD` | Complete 2026-09-23 |
| **Session 06** | Standalone | R388(b), control authority. <br>• Every resolved control carries its provenance: `engine_default`, `posture_default`, `derived_floor`, `weather_derived`, `operator_relaxable` or `operator_never_relax`. <br>• New `--never-relax` on BS, passed through by AB. <br>• Distinct lineups per contest is `operator_never_relax` by default (F-3); DG L101-108's comment, which still calls a same-contest duplicate "a DK rejection", is corrected with it. <br>• `merge_open_controls` (DG L331-343) honours it and records before, after, provenance and reason. <br>• Correct DG's stale docstring: weather caps ARE merged by min (EP L3088-3098). <br>• A typed cap alone is relaxable. | audit §2 "explicit prohibitions"; DD-07 prerequisite; F-3 | Strategy (S) | `DG`, `BS` (controls parse), `EP` (control resolution), `AB`, `DKM` (L1074-1079 reclassified) | `UT test_core.ControlProvenanceTests test_core.DeadlineGovernorTests test_core.DeadlineGovernorCliTests`; `GOLD` | Complete 2026-09-23 |
| **Session 07** | Batched | R207 + R396(b) + R345, typed refusals and the exit contract. <br>• (a) R207 (+R204 naming half, +R125(a) refusal text): `refusal.class` and `refusal.remedy` are typed from `gate_classes`. They are NEW fields: `errors[]` stays byte-identical, because the golden `pure_verdict` freezes its text. <br>• (b) NF-3: exit 5 gets its own handler, and `--max-attempts` exhaustion writes a stop record. <br>• (c) R345: AB forwards `--declare-pitcher`. <br>Needs 05. | R207, R345 (was CC-11 b3-b4); NF-3; DD-05 code half | Split Mixed | `AB`, `BS` (REFUSAL_SITES), `EP` (refusal payload), `CA` | `UT test_core.TypedRefusalTests test_core.RefusalClassificationTests test_core.SupervisorLostWindowTests`; `GOLD`. Breakpoint: (b) and (c) ship alone. | Complete 2026-09-23 |
| **Session 08** | Standalone | R393(b) + R297(c), the last-usable-artifact contract. <br>• A run result object carries the best current essential-valid artifact (bytes, sha256, coverage, label, accessible path). <br>• One new exit code means "delivered a prior valid artifact after a later failure". It is agreed with AB's table (0 delivered, 4 nothing to decide, 10 re-run; AB L652-667) and with BS's refusal-record writer (L6019). <br>• A brief or report exception cannot withhold the file; the file is presented before narrative. <br>• R297(c): a crash between `create_run` and the first refusal reconciles to a terminal state instead of stranding `building`. <br>Needs 02, 03, 07. | Package D; audit §8 recovery contract; R297(c) (was CC-33 b1) | Validity (V) | `BS` (main exit paths), `EP`, `BSM`, `DKM`, `AB` | `UT test_core.LastUsableArtifactTests test_core.SupervisorLostWindowTests` (a raising brief still delivers the file; a crash after create_run ends terminal) | Complete 2026-09-23 |
| **Session 101** | Standalone | R414: late swap gets Session 08's last-usable-artifact contract. <br>• The swapped file is presented before any narrative. <br>• A later exception (a raising promotion, the consensus-summary lines) or a failed record exits 7 with the file under its own label, the downgrade label when one was accepted. <br>• `__main__` exits through a recording door. <br>• Rider (Session 09): a swap whose own failures are all S or P is refused and not mirrored; mirroring it UNCERTIFIED needs the swap's never-relax set in `run_late_swap`. <br>Needs 08. | Session 08's R233 enumeration | Validity (V) | `LS` | `UT test_core.LateSwapLastUsableTests` | Pending |
| **Session 09** | Batched | R388(d) + R124 S-half + R268, the review-grade lifecycle. <br>• (a) When every failing gate is S or P, `execute_portfolio` (EP L309-500) mirrors `candidate/DO_NOT_UPLOAD_DKEntries.csv` as `outputs/<date>/DKEntries_<tag>_UNCERTIFIED_<run>.csv`, records certification `review_grade_uncertified` (bare `review_grade`'s preflight reason is Showdown's), and names the failing gates in a NEW export key (`review_grade_export.failing_gates`). `passed=False` and `errors[]` stay verbatim (`test_golden_replay.py` L585-632). Nothing is copied to `final/`, and `promote_run` is never called (BSM L418-426). `allocation_method` outside the allowlist becomes P with V revalidation. <br>• (b) R268: `run_late_swap` accepts a review-grade parent from any manifest run (EP L710) and inherits its realized controls, so a review-grade file can still be refined. <br>Needs 05, 08. | DD-04; Package A; R124 S-half (was CC-39); R268 (was CC-13 b1) | Split Mixed | `EP`, `DKM`, `UM`, `LS`, `LSM`, and `BS`, `PF` (the refusal payload and the preflight reason, found needed) | `UT test_core.ReviewGradeExportTests test_core.LateSwapReviewParentTests test_upload_integrity.GateAssumptionVersusOverrideTests`; `GOLD` | Complete 2026-09-24 |
| **Session 10** | Standalone | R389(a), the baseline core: a pure function plus unit tests, not wired in yet. <br>• In `mlb_engine/pipeline/baseline.py`: one unenriched-frame helper (`unenriched_frame`) shared with BS's ValueError branch (L3104), built on `EP._assemble_projection_frame(..., "emergency_proxy", None, None, None, projected_order_by_player_id=...)`. <br>• Candidate #1 is the probe; then distinct `OPT` MILP solves under time limits (never a player cut) until every reserved row of the largest contest is covered, or the window ends, which returns a typed short count. <br>• Uses an in-memory `BankCache` (`_MemoryBankCache`, no path, no save), not the shared `bank_path`, which tombstones jobs on a projection-digest change (`drop_stale_jobs`, BC L214). <br>• The pattern is copied from `mlb_engine/production/workflow.py` L233-321; tools may not import production. <br>• Found: `EP` needed no edit, and there is no gap limit to set (`build_single_lineup` passes `time_limit` only); the gap knob is a rider on R389(b). <br>Needs 06. | DD-01; Package B; R302 components (copy, not import) | Validity (V) | `mlb_engine/pipeline/baseline.py` (new), `BS` (shared helper), `tests/test_core.py` (`BaselineCoreTests`, the R293 census), `tools/audit.py` | `UT test_core.BaselineCoreTests` (distinct per contest; deterministic under `PYTHONHASHSEED=0`; no pool trim); `GOLD` | Complete 2026-09-24 |
| **Session 11** | Standalone | R389(b), baseline-first Classic. <br>• `run_classic` publishes the core's baseline after the pool-blocker refusals and the `--leverage` check, before `resolve_reference_data`: EP `run_baseline` builds it on `unenriched_frame` (every reserved row, a quarter of the window left, the build's `--max-opposing-hitters-per-sp`) and allocates it through `run_slate(candidates_override=..., delivery_lineage="baseline")` with the all-None enrichment dict and every cap opened by `merge_open_controls`, never-relax held. <br>• BS `publish_baseline` re-reads it on its exact bytes (`verify_classic`, essential-valid, the baseline label), writes a sha-bound brief, presents it and keeps it in `_LAST_USABLE`, so a crash after it exits 7 with it; `has_deliverable` still reads only the enhanced solves. <br>• `DKEntries_<tag>_BASELINE_<run>.csv`, `review_grade_baseline`, in its own manifest lineage (UM keys on `(contest_type, slate_tag, lineage)`), so the enhanced file supersedes nothing Ben holds; a late swap records in its parent's lineage and retires the others. <br>• Departures, approved at plan review: `resolve_leverage` moved above the baseline (exit 4 stays before any solve); refusals keep exit 3 and 10 and carry the baseline; the probe stays (measured); `late_swap.py`, `promote_run.py`, `outcome_review.py` and `preflight_upload.py` follow the lineage. <br>Needs 02, 03, 06, 08, 09, 10. | DD-01; Package B | Validity (V) | `BS`, `EP`, `UM`, `mlb_engine/pipeline/baseline.py`, `tools/preflight_upload.py`, `tools/late_swap.py`, `tools/promote_run.py`, `tools/outcome_review.py`, `skills/generate-lineups/SKILL.md`, `references/baseline.md` | `UT test_core.ClassicBaselineFirstTests` (a crash after the baseline delivers the baseline; the enhanced file supersedes nothing Ben holds; the baseline is essential-valid); `GOLD`; `PROBE` | Complete 2026-09-24 |
| **Session 12** | Standalone | R389(c), baseline-first Showdown. <br>• After pool pricing and before the thesis ladder, `run_showdown` writes a baseline: one thesis-free solve per incomplete reserved row (complete rows preserved, both-team rule enforced), validated and published review-grade under its own name. <br>• The ladder can replace it only with a validated file. <br>Needs 11. | DD-01; Package B | Validity (V) | `BS` (run_showdown L3698-4754), `SD` | `UT test_showdown.ShowdownBaselineFirstTests test_showdown.ShowdownSolverStatusTests` | Pending |
| **Session 13** | Standalone | R388(c), the essential-validity verdict. <br>• PF tags each of its 49 `rep.fail` sites with a `gate_classes` id and emits `essential_valid`. <br>• Bookkeeping-only failures (manifest row missing or stale, count or contest fields, status write; L1496-1588) get a distinct exit 6 ("essential-valid, record incomplete") instead of 2. V checks on exact bytes are unchanged. <br>• Updated in the same commit: CLAUDE.md's pre-upload sentence, the SKILL.md exit table (pinned at `test_upload_integrity.py` L2398-2411), and `VE`'s shared `verdict_exit_code` (PF L569). <br>Needs 05. | DD-08 (preflight half); Package A | Split Mixed | `PF`, `VE`, `CLAUDE.md`, `skills/generate-lineups/SKILL.md` | `UT test_upload_integrity.PreflightEssentialVerdictTests`; `python -m pytest tests/test_upload_integrity.py -k PreflightContractDocumentation -q` | Pending |
| **Session 14** | Batched | R175 + R178, the validators. <br>• (a) `VE` gains the contest-identity check and stops downgrading manifest failures (+F20). <br>• (b) Checker exit-contract smalls: a `csv.Error` traceback; the declared-arm acknowledgement is Classic-only. <br>Needs 13. | R175 (was CC-32 b1); R178 (old Session 55) | Validity (V) | `VE`, `PF` | `UT test_upload_integrity.VerifyExportIdentityTests` | Pending |
| **Session 15** | Standalone | R98(3) + R98(4)-tail / F18, one deadline, on every build (F-4). <br>• A frozen `Deadline(end, reserve)` is created once in BS main. `end` = min(`--deliver-by`, first lock minus F-1's 5 minutes), bounded by `--max-seconds`. <br>• `reserve` is the publication phase, measured once at the maximum entry count with `tools/benchmark_engine.py` and stored in `repo_env.host_profile` (R349). It is never a literal and never a runtime call (benchmark_engine imports production). <br>• `slice(requested)` replaces the leftover-clock sites (BS L2541, L2564, L2570, L2789, L5572) and BC's per-job limits. <br>• DG's `in_window` derives from the reserve; `WINDOW_MINUTES = 6.0` becomes a labelled fallback. <br>• The brief records stage timestamps and whether a cap was deliberate or starved. <br>Needs 06. | DD-02; Package B; R98(3)/F18 (was CC-17, elevated ahead of R87); F-1, F-4 | Validity (V) | `mlb_engine/pipeline/deadline.py` (new), `BS`, `DG`, `BC`, `mlb_engine/repo_env.py` | `UT test_core.DeadlineContractTests test_core.DeadlineGovernorTests test_core.DeadlineGovernorWiringTests test_core.DeadlineGovernorCliTests`; `PROBE`. Acceptance: an injected slow stage cannot consume the reserve, and no retry resets the deadline. | Pending |
| **Session 16** | Batched | R98(3) remainder + R204 ceiling half, the deadline reaches the solvers. <br>• (a) Showdown already has the governor (BS L3869-4009) but no solve budget. Every Showdown solve now takes a slice (SD L980, SDT L1775). <br>• (b) AB's child timeout is min(`per_build_seconds + 90`, remaining minus reserve) (L522, L612), and no attempt resets it. R204: grow-the-bank is bounded by measured per-solve cost and the remaining window. ("Bank at its ceiling" and the 1,536 ceiling landed with R415, Session 102.) <br>Needs 15. | DD-02; R204 (was CC-13 b2) | Validity (V) | `BS` (run_showdown), `SD`, `SDT`, `AB`, `BC` | `UT test_core.DeadlineThreadingTests test_core.SupervisorLostWindowTests test_showdown.ShowdownSolverStatusTests`. Breakpoint: (a) and (b) each ship alone. | Pending |
| **Session 17** | Batched | R98(3) remainder + R218, the deadline reaches the network. <br>• (a) The 31 network call sites across 11 files are admitted only when their timeout fits before the reserve; an optional fetch that cannot fit is skipped and recorded. <br>• (b) R218 and the 29 subprocess calls without `timeout=` across 10 files: bounded. <br>Needs 15. | DD-02; R218 (old Session 44) | Validity (V) | `LDA`, `SIM`, `BS`, `tools/` subprocess sites | `UT test_core.FetchAdmissionTests test_core.BoundedSubprocessTests` | Pending |
| **Session 18** | Standalone | R390 + R285 + R311, a thin bank delivers. <br>• At the exit-10 site (BS L2670-2689), when the thin bank can cover every reserved row under never-relax (F-3), the build goes on to allocation with reuse relaxed, labelled `bank_thin_delivered`. DG is consulted before any return. <br>• R285, whole: the direct path writes its job facts. One extractor reads `solve.bank`, `bank_exploration` and `bank_diagnostics` for BS L199, LS L290 and AB L420/L696 (NF-1). A stop persists the child's returncode, stdout, stderr and parsed brief. <br>• R311: the interaction-bind refusal names a derivable floor or says BANK-LIMITED. <br>Needs 07, 15. | DD-03; Package C; R285 (was CC-11 b1) +NF-1; R311 (was CC-11 b2) | Strategy (S) | `BS`, `AB`, `LS`, `BC`, `CA` | `UT test_core.ThinBankRecoveryTests test_core.SupervisorLostWindowTests` (a thin sliced bank with coverable entries produces a file inside the deadline); `PROBE` | Pending |
| **Session 19** | Standalone | R391(a), Classic relaxation that finishes. <br>• A deadline-admitted policy opens every control not marked never-relax: per-game caps including weather-derived (EP L3088-3098), candidate reuse (CA L3083-3103), `max_opposing_hitters_per_sp`, ownership requirements, allowed-candidate routing (CA L2210), stack floors and the five-stack quota. <br>• A timeout simplifies, recorded `time_pressure` and never as infeasibility; the same strategy is not re-run. <br>• Every move records its provenance and before/after values. <br>• The allocator's re-entry ladder (`max_candidate_reuse`, the five-stack quota, `primary_stack_min_size`) and its sleeve fallback read never-relax; until they do, `resolve_never_relax` refuses those names at BS exit 4 (R388(b)). <br>• No universal order: the smallest move that fits the remaining time. <br>Needs 06, 15, 18. | DD-07; Package C; R125(c) | Strategy (S) | `DG`, `EP` (`_merged_controls_for_build` region), `CA` | `UT test_core.RelaxationPolicyTests test_core.AllocatorSolverStatusTests`; `GOLD`; `PROBE` | Pending |
| **Session 20** | Batched | R391(b) + R203 + R244 + R132, the Showdown cap and the supervisor rescue. <br>• (a) Showdown `max_cpt_per_contest` joins the policy (BS L3755; default 2 at SD L83), and Showdown's R153 per-slot ladder reads never-relax; until it does, only F-3 is accepted on Showdown (R388(b)). <br>• (b) AB learns R157's rescue: sanity-check at 1.0, re-derive from structural floors, never ship 1.0, and open only the one cap the 1.0 check names (R244). It reads Session 07's typed remedy and the BANK-LIMITED line first (R132). <br>Needs 19. | DD-07; R203 +R132 +R244 (was CC-16) | Strategy (S) | `SD`, `BS` (run_showdown), `AB` | `UT test_core.ExposureCapRescueTests test_showdown.ShowdownCaptainCapPolicyTests`. Breakpoint: (b) ships alone. | Pending |
| **Session 21** | Batched | R392 + R125(b) + R66, optional inputs degrade instead of stopping. <br>• (a) The exit-4 sites in BS main (a missing ownership or leverage file, an invalid captain prior or sleeve, unreadable supplied Showdown projections, a strategy flag unsupported for the format) become a recorded degraded model or an auto-corrected flag. <br>• (b) R125(b): an unresolved contest posture (EP L1045, L1476) with a known Entry ID and contest takes a labelled default posture. It ships review-grade only and never certifies (EP L5177-5183 stays a no-override gate). <br>• (c) R66: silent API-path defaults become named ones. <br>• (d) R124(b) `--deliver-always`: never exit without either a certified file or a labelled uncertified legal one, stating which. Session 09 already mirrors an S/P-only refusal UNCERTIFIED; this is the exit contract over it. <br>The name-crosswalk refusal (BS L2340-2362) and the V inputs (salary, entries, draftgroup) still block. Needs 05, 09. | Package C; audit §3 refusal table; R125(b); R66 (old Session 53); R124(b) (from Session 09) | Process/Model (P) | `BS` (main, from L5315), `EP` (posture resolution) | `UT test_core.OptionalInputDegradeTests test_core.RefusalClassificationTests` | Pending |
| **Session 22** | Standalone | R401, partial salvage in the removed-rows form (F-2). <br>• Classic rung 2: a caller-set `require_all_reserved_filled` (default True; EP L451, L486), with the blank-row error moved out of roster legality into its own coverage fact (DKM L977). <br>• When the joint allocation fails (CA L3643-3687, all or nothing), allocate per contest group with the same scipy MILP and deliver the groups that solve. Unresolved rows are removed from the file and listed by Entry ID. <br>• PF validates the remaining rows against the template and reports coverage separately from invalidity (L770-774). <br>• The executed Showdown refusal test R290 asked for is added. <br>The pin `test_rung_two_is_unavailable_on_classic_and_the_refusal_says_so` is rewritten on purpose. Needs 09, 13, 19. | DD-06; Package C; R401 absorbs R290's Classic rung-2 remainder (R290 is CLOSED, numbers never reused); F-2 | Split Mixed | `EP`, `CA`, `DKM`, `PF`, `BS` | `UT test_core.PartialSalvageTests test_showdown.ShowdownRefusalExecutedTests test_upload_integrity.PartialFilePreflightTests` | Pending |
| **Session 23** | Batched | R393(a) + R245/R269 + R283 + R120, transactional publication. <br>• (a) `UM.deliver` publishes a validated version atomically. A manifest failure leaves a reconstructable receipt, never the only valid file stranded as `DO_NOT_UPLOAD_` with no named path (L426-442). <br>• (b) A manifest scope key, a CAS on `record_delivery`, and one name per re-promotion. <br>• (c) R283: `--past-slate-replay` cannot overwrite the delivered mirror. <br>• (d) R120: a delivered file names a run directory that exists. <br>Needs 02, 08. | DD-08 (manifest half); Package D; was CC-35 b1-b3 | Split Mixed | `UM`, `tools/promote_run.py`, `BSM`, `BS` (delivery path) | `UT test_core.PromotePointerCasTests test_core.DeferredPromotionTests test_upload_integrity.TransactionalPublicationTests` | Pending |
| **Session 24** | Batched | R394 + R121, delivery telemetry and receipt. <br>• (a) DR events: `run_started`, `requested_delivery_deadline`, `relevant_locks`, `effective_deadline`, `baseline_written`, `essential_validation_passed`, `artifact_hash`; entries requested, preserved, usable_delivered and unresolved; relaxation events with provenance; `run_terminal` for interrupted runs. <br>• (b) A new `tools/handoff.py` records `presented_at` and the channel when the file is handed into the conversation (presentation, not upload). Once presented, a file is frozen against silent supersession. `tools/retro.py` computes `first_usable_presented_at` and the margin. <br>• (c) R121: every clock figure is a measurement. <br>Needs 02, 08, 15. | Package E; audit §9; R121 | Process/Model (P) | `DR`, `tools/retro.py`, `tools/handoff.py` (new), `UM`, `skills/generate-lineups/SKILL.md`, `.claude/hooks/guard_commands.py` | `UT test_core.DeliveryTelemetryTests test_core.RetroFactsTests` | Pending |
| **Session 25** | Standalone | R395(a), acceptance part 1. <br>• Audit §7 scenarios 1-6 run through the real BS main → export → PF → handoff record on the vendored 2026-06-03 slate: short window; optional feed unavailable; preferred cap infeasible; uniqueness or stacking unsatisfiable; solver timeout with a legal incumbent; QA finds no improvement. <br>• They are added to `skills/generate-lineups/evals/run_evals.py`, which already checks exit codes and artifacts and runs outside the gate. <br>• Only a fast subset is gated, as a new suite in `AUDITED_SUITES` and `EXPECTED_SUITE_COUNTS` (`audit.py` L49, L129); state the gate wall time before and after. <br>Needs 01-24. | Package F | Validity (V) | `skills/generate-lineups/evals/run_evals.py`, `tests/test_delivery_acceptance.py` (new), `tools/audit.py` | `python skills/generate-lineups/evals/run_evals.py --only delivery` all pass; `PYTHONHASHSEED=0 MLB_DFS_ARTIFACT_ROOT="$(mktemp -d)" python -m unittest tests.test_delivery_acceptance -v`. Breakpoint: per scenario. | Pending |
| **Session 26** | Standalone | R395(b), acceptance part 2. <br>• Scenarios 7-12: an optional certification step fails; enhancement crashes after the baseline; one group fails while the others are feasible (removed-rows file); feasible only with weaker diversity (distinct kept); a source repeatedly times out; the explanation would consume the window. <br>• Plus: a hard process interruption, corrupt optional metadata, and mandatory-rule failures that must still refuse. <br>• Degraded-vs-normal quality is recorded as labelled review proxies. <br>Phase D is production-ready only when 25 and 26 pass. | Package F | Validity (V) | same as Session 25 | same as Session 25 | Pending |

### Phase V: validity truth

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 27** | Standalone | R279: wire `observed_starter_state`. Three sites derive "confirmed" from a feed that stops carrying it after first pitch, so a dead bat reads as an unposted side. | was CC-14 | Validity (V) | `LDA`, `EP`, `LSM` | `UT test_core.ObservedStarterStateTests` | Pending |
| **Session 28** | Batched | R325 + R174 + R404, lock truth. <br>• (a) `VE` resolves its lock feed by team set; a feed can unlock a salary-clock lock; explicit DK ids go unchecked. <br>• (b) The locked-team ban holds post-export, and LS runs the preflight instead of printing it. <br>• (c) R404: LS accepts "no feed, DK `Starting` covers every side" instead of exiting 4 on a missing `lineups_feed.json` (R143 ranks DK above any feed). `tools/repair_entry.py` takes confirmation from the feed synthesized from the salary file, as PF already does, and falls back to salary Game Info for locks, as VE does. On 1905_10g it returned no legal replacement for all five benched slots. <br>This row blocked a live repair; take it first in Phase V. | was CC-32 b2-b3; inbox `2026-09-22_BUILD_1905-late-swap-needs-feed-dk-starting-ignored` §1-2 | Validity (V) | `VE`, `LDA`, `LS`, `LSM`, `tools/repair_entry.py` | `UT test_upload_integrity.LockFeedTruthTests test_core.LateSwapPreflightTests test_core.RepairDkStartingTests` | Pending |
| **Session 29** | Batched | R284 + R68 + R141, late-swap rails. <br>• (a) A leverage cap binds at refine (+R293 rider i). <br>• (b) The downgrade guard is inert on "Name (ID)" cells; hostile flag and feed parsing. <br>• (c) A post-lock leverage pass, report-only. | R284 (was CC-35 b4); R68 (old Session 53); R141 (old Session 55) | Split Mixed | `LS`, `LSM` | `UT test_core.LateSwapRailsTests` | Pending |
| **Session 30** | Batched | R248 + R242 + R82 + R108, identity boundaries. <br>• (a) Team-code crosswalk in the preflight feed matcher (AZ/ARI). <br>• (b) Salary auto-resolve stays inside its date and draftgroup (+R303 superset rider). <br>• (c) One team-code boundary with an unknown-code report. <br>• (d) One DK `Starting` token boundary. | R248, R242 (was CC-34); R82, R108 (old Session 56) | Validity (V) | `PF`, `mlb_engine/team_codes.py`, `LDA` | `UT test_upload_integrity.IdentityBoundaryTests` | Pending |
| **Session 31** | Batched | R346 + R317 + R319, intake truth. <br>• (a) A hand-written feed without `game_date_utc` zeroes the DK batting order. <br>• (b) R317 (+(d)): age the platoon reference per TEAM so a partial refresh cannot silence the staleness check for all thirty. The refresh transport lands in Session 36. <br>• (c) Two paste name-form defects. | was CC-15 b1-b3; retained fragment `2026-09-04_BUILD_fangraphs-platoon-refresh-procedure`; greenfield 09-19 §1.5 | Split Mixed | `LDA`, `tools/fetch_fangraphs_platoon.py`, `SIM`, `mlb_engine/intake/paste_lineups.py`, `tools/lineups_from_paste.py` | `UT test_core.IntakeTruthTests test_paste_lineups` | Pending |
| **Session 32** | Batched | R398 + R125(a) + R189(3), who is actually pitching. <br>• (a) When a side is PO and a declared bulk arm exists, F4 grades against the bulk arm or a PA-weighted blend (`LDA.extract_opposing_probables` L1423), and `pool_report` names the substitution. <br>• (b) R125(a): declare the higher-salaried arm on PO+PLR and surface ties; graded against realized fpts in `data/archive/*/mined_*.json` before it ships. Its refusal-text half rides here too (declined from Session 07, where it had been slotted: the text is LDA's): the no-starter refusal names the PO arm, its price and `--declare-pitcher`. <br>• (c) Two mlb.com paste shapes drop both probables. | inbox `f4-grades-against-opener-not-bulk-arm`; R125(a) +grading (old Session 50); R189(3) (old Session 51) | Process/Model (P) | `LDA`, `PB`, `tools/lineups_from_paste.py` | `UT test_core.BulkArmMatchupTests test_paste_lineups`. Breakpoint: (a) ships alone. | Pending |
| **Session 33** | Batched | R332 + R148(b) + R399(b) + R122 remainder, handedness and platoon. <br>• (a) `tools/handedness_paste.py` plus `data/reference/handedness.csv`, aged per player. <br>• (b) Handedness reported per hitter, not per side. <br>• (c) `showdown_handedness` misses accented names (reuse the NFKD normalization; WARN). <br>• (d) The platoon factor dies silently at hitter intake. | R332 (was CC-15 b5); R148(b) (old Session 57); inbox `showdown-1915` item 2; R122 (old Session 51) | Process/Model (P) | `tools/handedness_paste.py` (new), `data/reference/handedness.csv`, `BS`, `PB` | `UT test_core.HandednessTests` | Pending |

### Phase C: capture transport (Ben, 2026-09-22 plan review: retire keyed APIs; the session's own web tools pull odds, weather and the rest)

The session becomes the transport, and the engine reads only captured bytes:
- The session fetches a page with its own web tools (WebFetch/WebSearch in a cloud container; a browser tool where the host has one).
- It saves the raw bytes with a provenance sidecar.
- An offline parser turns the capture into the shape the engine already consumes.

Rules for every capture:
- Never a DraftKings page, DFS or sportsbook (DK wall).
- A missing or unreadable capture is `not_checked` (P). It is never a neutral recorded as checked.
- Captures are optional work: skipped inside T-20, and admitted by the deadline (Session 15).
- This container's egress probe reaches none of the six live hosts today, so F1 and weather are neutral on every cloud build until this phase lands.

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 34** | Standalone | R402(a), the capture contract, and odds. <br>• Measure first: which odds, weather, lineup and stats sources the session's web tools can actually read. The 2026-09-22 odds fragment saw WebFetch refuse one odds page. Record the result per source in `docs/hosts.md`. <br>• `data/slates/<date>/captures/` holds each raw page plus a sidecar (`url`, `fetched_utc`, `sha256`, `tool`). A new `tools/capture.py record/list` registers them. <br>• `tools/odds_from_paste.py` parses a captured non-DraftKings odds page into the shape `parse_the_odds_api_totals` consumes (absorbs R236(b), the raw-capture half). <br>• BS `--odds-capture` feeds `load_odds_packet` (L1424). <br>• SKILL.md gains the capture step. | Ben 2026-09-22; R236(b) (old Session 49); R367 egress probe; inbox odds fragment side note | Process/Model (P) | `tools/capture.py` (new), `tools/odds_from_paste.py`, `BS`, `docs/hosts.md`, `skills/generate-lineups/SKILL.md` | `UT test_core.CaptureContractTests test_paste_lineups.OddsCaptureTests`. Breakpoint: the measurement plus the contract ship alone. | Pending |
| **Session 35** | Batched | R402(b), weather and the slate by capture. <br>• (a) A new `tools/weather_from_paste.py` parses a captured forecast (Open-Meteo JSON or a weather.gov page) into `build_f5_map`'s input (BS L1678). <br>• (b) Captured statsapi schedule, probables and lineups JSON is parsed into the mlb-lineups feed shape. `tools/lineups_from_paste.py` already covers mlb.com pages. <br>Needs 34. | Ben 2026-09-22 | Process/Model (P) | `tools/weather_from_paste.py` (new), `tools/lineups_from_paste.py`, `BS`, `LDA` | `UT test_paste_lineups.WeatherCaptureTests test_paste_lineups.StatsapiCaptureTests` | Pending |
| **Session 36** | Batched | R402(c) + R317 transport half, reference data by capture. <br>• (a) FanGraphs platoon roster grids are captured and fed to `tools/fetch_fangraphs_platoon.py --from-dir`. This is R317's transport, which retires the retained 2026-09-04 fragment. <br>• (b) Savant and FanGraphs season-stat tables are captured into `data/reference/` with their sidecars. <br>Needs 31, 34. | Ben 2026-09-22; R317 (was CC-15 b2); retained fragment `2026-09-04_BUILD_fangraphs-platoon-refresh-procedure` | Process/Model (P) | `tools/fetch_fangraphs_platoon.py`, `tools/capture.py`, `data/reference/` | `UT test_core.PlatoonCaptureRefreshTests` | Pending |
| **Session 37** | Standalone | R402(d), retire the keyed and in-build network paths, once two BUILD slates have run on captures (recorded in the ledger). <br>• Remove the `THE_ODDS_API_KEY` path: the-odds-api and odds-api.io callers in `BS`, the odds leg of `tools/fetch_slate_bundle.py`, and `skills/generate-lineups/evals/run_evals.py`. <br>• Remove the live Open-Meteo and statsapi calls inside the build; a capture or a labelled `not_checked` replaces each. <br>• CLAUDE.md's key line names `GH_PAT` only. `docs/hosts.md` updates its secrets row. `repo_env.resolve_secret` loses the odds consumer. <br>• Session 17's fetch admission shrinks to whatever remains. <br>Needs 34-36. | Ben 2026-09-22 | Process/Model (P) | `BS`, `LDA`, `tools/fetch_slate_bundle.py`, `skills/generate-lineups/evals/run_evals.py`, `mlb_engine/repo_env.py`, `CLAUDE.md`, `docs/hosts.md` | `grep -rn "THE_ODDS_API_KEY\|api.the-odds-api\|api.odds-api.io\|api.open-meteo" mlb_engine tools skills --include=*.py` returns no live caller; `UT test_core.NoInBuildFetchTests`; `GOLD` | Pending |

### Phase T: throughput (margin inside the window)

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 38** | Standalone | R87: solver throughput under the golden gate (decision first, as filed). | was CC-17; old Tier 6 | Process/Model (P) | `OPT`, `BC` | `GOLD`; `PROBE`; `python tools/benchmark_engine.py --live` before and after | Pending |
| **Session 39** | Standalone | R321: a parallel bank. The bank stage is 91-98% of wall clock across 57-383 serial solves. The merge is sorted (`stable_union`) and `PYTHONHASHSEED=0` is kept. Needs 38. | was CC-17 | Process/Model (P) | `BC`, `mlb_engine/determinism.py` | `GOLD`; `PROBE`; benchmark before and after | Pending |
| **Session 40** | Batched | R115 + R164 + R138, bank coverage. <br>• SP-pair coverage: the bank freezes at `requested_n` and no floor forces arm diversity. <br>• The job grid wastes solves in two ways. <br>• One stack family per game. | R115, R164 (was CC-36); R138 (old Session 57) | Strategy (S) | `BC`, `OPT` | `UT test_core.BankCoverageTests`; `PROBE` | Pending |
| **Session 41** | Batched | R73 + R84 + R130 + R71(b)(c)(d), solver and allocation observability. <br>• Budget and diagnostics waste. <br>• Per-control slack and per-entry coverage. <br>• A shared bank that grew silently. <br>• Allocator counted-honesty remainder. | R73, R84, R71 (old Session 53); R130 (old Session 54) | Process/Model (P) | `CA`, `BS` (brief), `BC` | `UT test_core.AllocationObservabilityTests` | Pending |

### Phase P: prize (measurement, then construction)

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 42** | Batched | R341 + R225 + R226 + R227, miner truth. <br>• Ownership from the entry block, not `%Drafted`. <br>• Captain-aware duplication at four sites. <br>• The winner is the verified rank-1 row. <br>• An atomic contest_library registry. | was CC-7 | Process/Model (P) | `FM`, `tools/ownership_grade_archive.py`, `PF` (L2330-2345), `mlb_engine/field/contest_library.py`, `docs/cowork_archival_runbook.md` | `UT test_core.FieldMinerTruthTests` | Pending |
| **Session 43** | Standalone | R342: separate availability from popularity in the ownership prior, graded on held-out dates before it reaches `--leverage`. Needs 42. | was CC-8 | Process/Model (P) | `mlb_engine/field/ownership_prior.py`, `tools/ownership_pred.py`, `tools/ownership_grade_archive.py` | `UT test_core.OwnershipPriorAvailabilityTests`; the held-out grade printed | Pending |
| **Session 44** | Standalone | R10: the hierarchical ownership fit (team stack share, then within-team split). Needs 43. | was CC-9 | Process/Model (P) | `mlb_engine/field/ownership_prior.py`, `tools/ownership_pred.py`, `FM` | `UT test_core.OwnershipFitTests` | Pending |
| **Session 45** | Batched | R48 + R258 mining half: a per-contest leverage table and `contest_thresholds`, from one re-mine. | was CC-10 b1-b2 | Process/Model (P) | `FM`, `data/reference/dk_contest_money_2026-09-15.json` | `UT test_core.ContestThresholdMiningTests` | Pending |
| **Session 46** | Batched | R83 + R201 + R251, the provenance freeze. <br>• Per-run calibration provenance (+F05). <br>• The small-slate false negative in `MIN_AUTO_TEAM_COVERAGE`. <br>• Freeze the skill and market inputs per slate. | was CC-10 b3; R251 (was unslotted, old Tier 2) | Process/Model (P) | `BSM`, `EP`, `FM`, `tools/awaiting_standings.py` | `UT test_core.CalibrationProvenanceTests` | Pending |
| **Session 47** | Batched | R276 + R197 + R309, QA panels: chalk anchor and the other mine riders; a key nobody writes; hand-applied factor provenance. | was CC-12 b2-b3; R309 (was CC-38 b3) | Process/Model (P) | `QA`, `tools/ownership_pred.py` | `UT test_core.QaPortfolioPanelTests` | Pending |
| **Session 48** | Standalone | R260: the correlation structure mined from the archive, first of the trio. | was CC-18 | Process/Model (P) | new `mlb_engine/field/` module | `UT test_core.CorrelationStructureTests` | Pending |
| **Session 49** | Standalone | R259: the variance basis, per-player nightly FPTS priors. | was CC-19 | Process/Model (P) | new `mlb_engine/field/` module | `UT test_core.VarianceBasisTests` | Pending |
| **Session 50** | Standalone | R258 predict half: the conditional threshold model, held-out. Needs 45. | was CC-20 | Process/Model (P) | `FM`, new predictor | `UT test_core.ThresholdModelTests` | Pending |
| **Session 51** | Standalone | R261: the own-portfolio scenario bank, reporting top-1% and washout review proxies pre-lock. It steers nothing until graded. Needs 48-50. | was CC-21 | Process/Model (P) | new module | `UT test_core.ScenarioBankTests` | Pending |
| **Session 52** | Batched | R238 + R239(a): Showdown resolves no contest shape; entry-to-contest assignment is still positional. | was CC-27 | Strategy (S) | `mlb_engine/contest_shapes.py`, `mlb_engine/field/contest_library.py`, `SDT`, `BS` | `UT test_showdown.ShowdownContestShapeTests` | Pending |
| **Session 53** | Standalone | R208: Showdown merges a confirmed batting order from `--lineups`. | was CC-28 | Validity (V) | `LDA`, `SD`, `BS` (run_showdown) | `UT test_showdown.ShowdownLineupMergeTests` | Pending |
| **Session 54** | Batched | R252 + R137: the mispricing score (a)-(c) and the market-vs-crowd divergence screen. | was CC-23; R137 (old Session 61) | Process/Model (P) | `PB`, `QA` | `UT test_core.MispricingScoreTests` | Pending |
| **Session 55** | Standalone | R254: the captain mispricing screen. Needs 52, 54. | was CC-24 | Process/Model (P) | `SDT`, `tools/ownership_pred.py` | `UT test_showdown.CaptainMispricingTests` | Pending |
| **Session 56** | Standalone | R209: the objective's leverage proxy, part C plus the calibration reading. Needs 44. | was CC-25 | Strategy (S) | `OPT` | `UT test_core.LeverageProxyTests`; `GOLD` | Pending |
| **Session 57** | Standalone | R140: opponent-conditioned field profiles for the recurring satellite families. Needs 44. | was CC-26 | Process/Model (P) | `mlb_engine/field/`, `mlb_engine/field/contest_library.py` | `UT test_core.FieldProfileTests` | Pending |
| **Session 58** | Standalone | R37(2)(c): the 4-2-x secondary cap, the only secondary control the mine supports sizing. | was CC-22 | Strategy (S) | `CA`, `OPT` | `UT test_core.SecondaryStackCapTests`; `GOLD`; `PROBE` | Pending |
| **Session 59** | Batched | R253 + R334(b) + R310 SHRINK half: park factor by handedness and profile; the Showdown opposing-arm term; the APPG Base for a sub-5-game callup. | was CC-29; R310 (was unslotted) | Process/Model (P) | `PB`, `SD`, `data/reference/` | `UT test_core.ParkFactorTests test_showdown.OpposingArmTermTests` | Pending |
| **Session 60** | Batched | R255 + R257: grade the skill signal; extend the kill matrix. | was CC-30 | Process/Model (P) | `FM`, `QA` | `UT test_core.SkillSignalGradeTests` | Pending |
| **Session 61** | Standalone | R339: the strangler adapter into `production.EvidenceBundle`, report-only. | was CC-31 | Process/Model (P) | `mlb_engine/production/adapters.py` (new) | `python -m pytest tests/test_production.py -q` | Pending |
| **Session 62** | Batched | R123 + R211 + R295 remainder, Showdown ladder strategy. <br>• A per-player cap and targeted captain relaxation. <br>• `pitchers_duel` rotation. <br>• F14's soft-lock field and the hold-ordering question. | R123, R211 (old Session 51); R295 (was unslotted) | Strategy (S) | `SDT`, `SD` | `UT test_showdown.ShowdownLadderStrategyTests` | Pending |
| **Session 93** | Standalone | R405: the consensus-cluster cap. <br>• (a) Report the bank-consensus cluster, per-lineup member counts and the k-or-more share on the Classic brief and in `QA`. <br>• (b) A joint-MILP row capping the share of entries at k or more members, S class under R386. <br>• (c) Cluster-limited bank jobs in their own conditions bucket, so low-cluster candidates exist. <br>• Designs and Ben's defaults settled 2026-09-23 (the entry's rider); a fresh session builds 93, 95, 96, 94 in that order. | new (Ben, 2026-09-23; answers R247(d)) | Strategy (S) | `CA`, `BC`, `OPT`, `BS`, `QA`; landed also in `EP`, `DG`, `LS`, `tools/solver_probe.py`, `tools/benchmark_engine.py` | `UT test_core.ConsensusClusterCapTests`; `GOLD`; `PROBE`. Breakpoint: (a) alone, then (a)+(b). | Complete 2026-09-23 |
| **Session 94** | Standalone | R406: Classic scenario sleeves (projection; salary-only; consensus-fails; one environment sleeve per high-total or high-park game), entries allocated across sleeves by declared weights. Needs 93 through (c). <br>• Designs and Ben's defaults settled 2026-09-23 (the entry's rider); a fresh session builds 93, 95, 96, 94 in that order. | new (Ben, 2026-09-23) | Strategy (S) | `BS`, `EP`, `BC`, new `mlb_engine/optimize/classic_sleeves.py`; landed also in `CA`, `DG`, `tools/solver_probe.py`, `tools/benchmark_engine.py` | `UT test_core.ClassicSleeveTests`; `GOLD`; `PROBE`. Breakpoint: sleeves built and reported with allocation unchanged. | Complete 2026-09-23 |
| **Session 95** | Standalone | R407: caps scale with input confidence; a deterministic tier from the brief's own degradation facts tightens the player cap and R405's cluster share, provenance `confidence_derived`, first to relax under R386. Needs 93(b) for the cluster half. <br>• Designs and Ben's defaults settled 2026-09-23 (the entry's rider); a fresh session builds 93, 95, 96, 94 in that order. | new (Ben, 2026-09-23) | Strategy (S) | `EP`, `BS` | `UT test_core.ConfidenceScaledCapTests`; `GOLD` | Complete 2026-09-23 |
| **Session 96** | Standalone | R408: projection vs salary vs APPG on the archive through `tools/replay_slate.py`: rank correlation and top-decile and tail hit counts per slate, observed outcomes only. Premise measured: about 5 archived slates carry a salary file. <br>• Designs and Ben's defaults settled 2026-09-23 (the entry's rider); a fresh session builds 93, 95, 96, 94 in that order. | new (Ben, 2026-09-23) | Process/Model (P) | `tools/replay_slate.py` (Ben chose the mode over a new tool; the planned `tools/projection_grade_archive.py` was not needed) | `UT test_core.ProjectionBackfillGradeTests`; the per-slate table printed | Complete 2026-09-23 |

### Phase G: guard and record

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 63** | Batched | Showdown brief and QA truth. <br>• (a) R363: the Showdown brief gets its `enrichment` block. <br>• (b) R237 + R320: uncomputed, computed-neutral, resolved and applied are four distinct factor states on every surface. <br>• (c) R383 + R399(d) + R210: QA stops reading Classic shapes on Showdown (captain tier, "F1 NEUTRAL", section 1, probe errors, the no-op `--postures`). <br>• (d) R399(c): `f1.prior.factor_by_team` records the pinned SP's 1.0. | R363, R383, R237 (were unslotted); R320 (was CC-15 b4); R210 (old Session 51); inbox `showdown-1915` items 3-4 | Process/Model (P) | `BS` (run_showdown brief), `QA`, `EP` | `UT test_showdown.ShowdownBriefTruthTests test_core.FactorStateTests` | Pending |
| **Session 64** | Batched | R379 + R373 allocation half + R224 + R247(b), Showdown ladder smalls. <br>• The floor rung re-solves an identical problem. <br>• An alphabetical "favorite". <br>• Cap-resolver smalls. <br>• Cap cost recorded as a proxy delta. | all were unslotted | Strategy (S) | `SDT`, `SD`, `BS` | `UT test_showdown.ShowdownLadderSmallsTests` | Pending |
| **Session 87** | Batched | R412, two gaps main's R295 (c12fd2a) left open that closed PR #18 had covered. <br>• (a) The melt's same-name role refusal (`SD` L337) runs before the participation and health filters, so two benched or OUT players stop a Showdown build. Scope it per PERSON from `_role_rows`: PR #18's per-key scoping drops a healthy twin beside an OUT one with no refusal (measured, 20 persons to 19). <br>• (b) The brief's `captain_budget` block carries `hold_yielded_to_lock` on both paths (`SDT` L2455 records it; `BS` L4819 drops it). | Ben 2026-09-23, review of closed PR #18 | Split Mixed | `SD`, `BS`, `tests/test_showdown.py` | `UT test_showdown.ShowdownRoleCollisionScopeTests test_showdown.ShowdownBriefHoldYieldedTests`. Breakpoint: (b) alone. | Pending |
| **Session 65** | Standalone | R166: fixed-row denominators for the post-R61 controls, Classic and Showdown (+F17). | was CC-37 | Strategy (S) | `CA`, `SDT`, `BS` | `UT test_core.FixedRowDenominatorTests`; `GOLD` | Pending |
| **Session 66** | Batched | R300(c)(d) + R79 + R91 + R230, tests that can fail. <br>• Behavioural tests replace string pins. <br>• Guards that cannot fail. <br>• A mutation smoke. <br>• Three seams that survive mutation. | R300 (was CC-38 b2); R79, R91 (old Session 52); R230 (old Session 48) | Process/Model (P) | `tests/`, `tools/audit.py` | `GATE` with the new pins; every new test mutation-checked | Pending |
| **Session 67** | Batched | R11 + R89 + R193 + R171, QA readers. <br>• The bounded skeptic pass. <br>• Checkpoint and artifact UX, with R124(c): the blocker collection is single-pass across pool, posture, feasibility and pre-export. <br>• QA reads whatever brief it is handed. <br>• QA reads clean when it is not. | R11, R89 (old Session 55), R124(c) (from Session 09); R193, R171 (old Session 54) | Process/Model (P) | `QA`, `BS` | `UT test_core.QaReaderTruthTests` | Pending |
| **Session 68** | Batched | Premise re-verification sweep with the `dfs-premise` agent. <br>• R36's container (F1m, F3m, F6m, F14, F2, F10, F8, F7, F12). <br>• R42(b). <br>• R61-tail. <br>• R312's portfolio_ev rider. <br>Each is closed with evidence or refiled with a corrected mechanism and a row. | R36 (was unslotted; old Tier 1 #13, Tier 3 #26); R42(b) (old Tier 6); R61-tail, R312 rider (Open tails) | Process/Model (P) | `docs/backlog.md`, `docs/ROADMAP.md`, `CHANGELOG.md` | `LINT`; each closure cites its grep | Pending |
| **Session 69** | Batched | R274 remainder + R231, host-bound cleanup: test rows in the `outputs/2026-06-03` and `outputs/2026-06-11` manifests; 1,778 leaked test dirs. **Host: Ben's Windows machine**, because `outputs/` is absent from a cloud clone. R231's sweep needs Ben's grant (Session 91). | R274 (was CC-33 b2); R231 (old Session 48) | Process/Model (P) | `outputs/2026-06-03/`, `outputs/2026-06-11/`, `UM` | Before and after counts printed | Pending |
| **Session 97** | Batched | R409: the session contract read against Anthropic's prompting guide for the current Claude model (claude.dev blog, read 2026-09-23). <br>• A finish line and a stop list in `/dev-session`; CLAUDE.md's Autonomy says a progress report is not a stop. <br>• `claims/<claim>/TASKS.md`, re-injected verbatim by the PreCompact hook; released claims stop reading as held; a re-take moves the earlier holder's list aside. <br>• Premise checks fan out to `dfs-premise`, and the parent re-runs the sharpest grep; an UNCHECKED verdict for what the tree cannot answer. <br>• A `/code-review` pass in `/land`; the closing report leads with what is blocked on Ben. <br>• Four stale "the push is Ben's" sites R353(e) missed. | Ben 2026-09-23 | Process/Model (P) | `CLAUDE.md`, `.claude/skills/dev-session/SKILL.md`, `.claude/skills/land/SKILL.md`, `.claude/skills/ship/SKILL.md`, `.claude/agents/dfs-premise.md`, `.claude/hooks/precompact_context.py`, `tools/claim.py`, `tools/audit.py`, `tests/test_core.py` | `UT test_core.RepoAgentsAndHookEventsTests test_core.ClaimToolTests` | Complete 2026-09-23 |
| **Session 98** | Batched | R410: the gate is described once per host. <br>• `skills/generate-lineups/SKILL.md` Session hygiene gives the one-call gate and points at `docs/hosts.md` for its time and `docs/cowork_sandbox.md` for Cowork's split gate. <br>• `/dev-session` step 3, CLAUDE.md's Hosts bullet and this file's Sizing line point at `docs/hosts.md` rather than carry a time. <br>• `.claude/rules/skills.md`'s line count; "40 modules" in the PR template and `.claude/rules/engine.md`. <br>• The Progress Ledger's backfill command returns the merge, not the commit. | Ben 2026-09-23 | Process/Model (P) | `skills/generate-lineups/SKILL.md`, `.claude/skills/dev-session/SKILL.md`, `.claude/rules/skills.md`, `.claude/rules/engine.md`, `.github/pull_request_template.md`, `CLAUDE.md`, `docs/hosts.md`, `docs/ROADMAP.md`, `tools/audit.py`, `tests/test_core.py` | `UT test_core.HostProseIsCurrentTests` | Complete 2026-09-23 |
| **Session 99** | Standalone | R411: two fixture evals fail on main and nothing runs them. <br>• Eval 2 (deadline refusal) fails from the R405 merge (afbaf68); eval 5 (satellite posture) already fails at fa8f343. <br>• The surface guard misses tracked `data/deliveries/`: one eval run left seven records there. <br>• Per eval: bisect, then re-pin with the reason in its notes or fix the regression. <br>• Decide whether a fast eval subset joins the gate (with R395(b) if it lands first). | found by Session 03's eval run, 2026-09-23 | Validity (V) | `skills/generate-lineups/evals/evals.json`, `skills/generate-lineups/evals/run_evals.py`, whatever the bisect names | `python skills/generate-lineups/evals/run_evals.py` prints 8 of 8 PASS | Pending |
| **Session 100** | Standalone | R413: `tools/plan_status.py` read a Session ID only as exactly two digits. <br>• The master-table row, NEXT and Progress Ledger patterns accept two or three digits (`SESSION_ID`). <br>• Rows sort by number, so `--print` puts 100 after 99. <br>• This row is the first three-digit ID and exercises the fix in the live file; 89 stays free. | Ben 2026-09-23 | Process/Model (P) | `tools/plan_status.py`, `docs/ROADMAP.md`, `tools/audit.py`, `tests/test_core.py` | `UT test_core.PlanStatusTests`; `LINT` | Complete 2026-09-23 |
| **Session 102** | Standalone | R415: the Classic sliced bank is growable on every host. <br>• The cap comes from `repo_env.bank_max_candidates`: 12 per entry at Cowork's 130s, scaled with the call budget, clamped to the measured full-solve ceiling (1,536). <br>• `--bank-max-candidates` overrides it, recorded with its source, and selects the sliced door. <br>• `bank_stop_reason` (candidate_cap, time_budget, jobs_retryable, job_list_exhausted) is on the brief and the refusal, every grow remedy names its lever, and exit 10 no longer loops at the cap. <br>• autobuild raises a capped refusal's cap to the ceiling, reads the refusal's `bank_exploration`, and moves a direct-door BANK-LIMITED refusal to the sliced bank. <br>• Measured first on 06-28 11g at 384/768/1,536/3,000. | Ben 2026-09-24; fragment `2026-09-23_BUILD_bank-cap-12-per-entry.md`; R204 ceiling wording | Validity (V) | `mlb_engine/repo_env.py`, `mlb_engine/optimize/bank_cache.py`, `BS`, `CA` (message text), `AB`, `tools/late_swap.py`, `tests/test_core.py`, `tests/test_golden_replay.py`, `tests/test_greenfield_regressions.py`, `tools/audit.py` | `UT test_core.BankCapGrowthTests test_core.AutobuildBankCapTests`; `PYTEST test_greenfield_regressions test_golden_replay`; `LINT` | Complete 2026-09-24 |
| **Session 103** | Standalone | R416: on the direct door, R407's confidence re-solve rebuilds `run_slate`'s auto-bank with a second full window. <br>• `run_bank_budget` is computed once before the first `_solve`, so every re-solve's bank gets the whole window again. <br>• On 06-28 11g (38 entries) at `--max-seconds 600` the build ran past 17 minutes and was killed; at autobuild's 105s it ran 219s, and under autobuild the child died at 195s with no brief, so no lever (R415's direct-door switch included) could act. <br>• Verify, then recompute the budget per call or reuse the first call's candidates, and check `projected_direct` against a measured 10+ game direct bank. | found measuring R415 | Validity (V) | `BS`, `EP` (`run_slate`'s `auto_bank_out`/`reuse_auto_bank`), `tests/test_core.py`, `tools/audit.py` | `UT test_core.DirectDoorResolveBankTests test_core.RunSlateAutoBankReuseTests test_core.DeadlineGovernorWiringTests`; `LINT`. Breakpoint: the budget fix ships alone. | Complete 2026-09-24 |
| **Session 104** | Standalone | R417: the direct door's wall clock is not the window's. <br>• (a) `projected_direct` put 60-73s on an 11-game bank that took 580s, fed by one warm single-lineup solve; feed it from the probe's 5-lineup measurement plus its consensus and sleeve terms, or send a slate past a measured cross-pair count to the sliced door. <br>• (b) The direct door's sleeves get `max(10, 0.30 x budget)` on top of the bank's; size their allowance from their need and carve it out. <br>• Measure on 06-28 11g and 06-03 2g first. | found measuring R416 | Validity (V) | `BS`, `EP`, `tools/solver_probe.py` | `UT` a direct-door choice that matches a measured bank on both slates; `LINT`. Breakpoint: (a) and (b) each ship alone. | Pending |
| **Session 105** | Standalone | R418: two record defects found measuring R416. <br>• (a) `anti_correlation_brief_block` reads `result["bank_diagnostics"]`, which no result carries, so every direct-door brief says `applied_source: unobserved`; read `candidate_bank`. <br>• (b) A same-date build overwrites `data/slates/<date>/DKSalaries.csv` under a running one; hand `run_slate` the tagged copy or refuse the second build. | found measuring R416 | Process/Model (P) | `BS`, `tests/test_core.py` | `UT` a real direct-door `run_slate` brief reads its anti-correlation solves; `LINT` | Pending |
| **Session 106** | Standalone | R419: autobuild names no file an exit-3 brief delivered. <br>• Since R389(b) a refusal after the baseline keeps exit 3 and carries the file under `baseline`; R388(d)'s UNCERTIFIED file rides under `review_grade_export`. autobuild reads a file only on exit 7, and its timeout and off-contract stops read none. <br>• Every stop presents the current file from the latest brief, or from `current_deliveries` after a kill. | found at Session 11's plan review | Validity (V) | `tools/autobuild.py`, `tests/test_core.py` | `UT` an autobuild run whose attempts all refuse after a baseline ends naming its file and sha | Pending |
| **Session 107** | Standalone | R420: `run_evals.py`'s surface guard omits the tracked `data/deliveries/`, so every eval run leaves records to remove by hand. | found at Session 11's plan review | Process/Model (P) | `skills/generate-lineups/evals/run_evals.py` | `UT` the guard restores `data/deliveries/` | Pending |
| **Session 108** | Standalone | R421: the instruction surface audited against the current Claude model (`/claude-api prompt-audit`). <br>• F1: the delivery-record commit names its paths. F2: a failing gate ships review-grade under the deadline rules instead of "not a deliverable". F3: seven gated suites. <br>• F4-F8 and L8: file-diff and history prose rewritten to the present rule with its R-number. <br>• F9: `/dev-session` step 6 keeps the premise check and drops the per-entry subagent mandate and the re-run. F10 and L6 riders. <br>• L5: `/land` and `/ship` are model-invocable. | Ben 2026-09-25 | Process/Model (P) | `CLAUDE.md`, `MLB_Classic.md`, `skills/generate-lineups/SKILL.md`, `skills/generate-lineups/references/late_swap.md`, `skills/generate-lineups/references/showdown.md`, `skills/generate-lineups/references/retro.md`, `.claude/rules/skills.md`, `.claude/rules/engine.md`, `.claude/skills/dev-session/SKILL.md`, `.claude/skills/land/SKILL.md`, `.claude/skills/ship/SKILL.md` | `UT test_core.HostProseIsCurrentTests test_core.RootContractBudgetTests test_upload_integrity.PreflightContractDocumentationTests` | Complete 2026-09-25 |

### Phase H: hygiene (the old Phase 6 fillers, renumbered; members that moved earlier are named where they went)

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 70** | Standalone | R216: the gate tree fingerprint omits `skills/` (remainder). | old Session 43 | Process/Model (P) | `tools/audit.py` | `UT test_core.AuditFingerprintTests` | Pending |
| **Session 71** | Batched | R217 + R180 + R202: four audit-gate holes; audit hardening; `solver_probe` reads the staged slate. R218 moved to Session 17. | old Session 44; R202 (old Session 54) | Process/Model (P) | `tools/audit.py`, `tools/solver_probe.py` | `UT test_core.AuditHardeningTests` | Pending |
| **Session 72** | Batched | R195 + R181 + R179: an explicit wta_satellite posture reintroduces large_wta; Eval 5's misattributed red; `build_asserted` bypasses the hash-seed pin. | old Session 45 | Split Mixed | `BS`, `tools/audit.py` | `UT test_core.PostureMappingTests` | Pending |
| **Session 73** | Batched | R299 remainder + R161 remainder + R177: six intake seams; two clock residues; empty vs None confirmed ids fail open. | old Session 46 | Validity (V) | `LDA`, `SIM`, `PF` | `UT test_core.IntakeSeamTests` | Pending |
| **Session 74** | Batched | R264 + R265 + R400: three odds-to-probability implementations; the paste tools' zero-network contract; the F3 factor with no producer (wire it or delete the term, EP L4206); two `sha256_file` implementations (BSM L42, UM L100). | old Session 47; greenfield 09-19 §1.4 | Process/Model (P) | `LDA`, `tools/odds_from_paste.py`, `EP`, `BSM`, `UM` | `UT test_core.SingleImplementationTests` | Pending |
| **Session 75** | Batched | R221 + R280, R222 + R75, R229: the DK merge stamps one leg's order on every doubleheader leg; `tail_candidate_scanner` has no leg filter and no production caller (wire it or delete it, greenfield 09-19 §1.4); the Savant name-join; two slate-intake silences. | old Session 47; greenfield 09-19 §1.4 | Validity (V) | `LDA`, `mlb_engine/optimize/tail_candidate_scanner.py`, `SIM` | `UT test_core.DoubleheaderLegTests` | Pending |
| **Session 76** | Batched | R232 + R241/R243 + R290(b) + R315(b): a retired manifest named in MLB_Classic.md; the `--postures` vocabulary plus the container recipe; `_plan_joint_allocation` feasibility inputs (EP L3475); two gitignore gaps. | old Session 48 minus moved members; R315(b) (old Session 50) | Process/Model (P) | `MLB_Classic.md`, `MANIFEST.md`, `BS`, `EP`, `.gitignore` | `UT test_core.PlanJointFeasibilityTests`; `GOLD` (the frozen `pure_verdict.errors` is this exact message) | Pending |
| **Session 77** | Batched | R303 tail + R271 remainder + R236(b) + R306 step 5: the tenth edition's hygiene tail; the audit gate defaults decision; odds paste of a raw Action Network capture; the bat-captain shape claim. | old Session 49 | Process/Model (P) | `tools/audit.py`, `tools/odds_from_paste.py`, a ledger fragment | `UT test_core.OddsPasteRawCaptureTests` | Pending |
| **Session 78** | Batched | R80 + R90 + R267(d) + R131: shared-helper hygiene; frozen-fixture intake tests; a build-time repair-feasibility count; five smalls (R131(a) is Ben's, Session 90). | old Session 52 minus moved; R131 (old Session 57) | Process/Model (P) | `mlb_engine/intake/`, `tests/` | `UT test_core.SharedHelperTests` | Pending |
| **Session 79** | Batched | R17 + R58(c)(d) + R162/R106. <br>• contest_library strips the ` SE` guard (wire it or delete it). <br>• Doubleheader per-leg orders and weather window. <br>• FanGraphs K-rate neutralization (+rider: retire the tracked CSV; a missing K-rate source becomes a named blocker). <br>• `--postures` cannot express ticket_count. | old Session 56 minus R82/R108 | Process/Model (P) | `mlb_engine/field/contest_library.py`, `LDA`, `PB`, `BS` | `UT test_core.KRateSourceTests` | Pending |
| **Session 80** | Batched | R76 + R77 + R88 + R31/R86: a doc-truth batch; dead code carrying live hazards; the DK wall mechanized as tests; claim reporting and the ownership token (R31(d) is Ben's, Session 90). | old Session 58 | Process/Model (P) | `docs/`, `tools/claim.py`, `tests/` | `UT test_core.ClaimToolTests test_core.DkWallTests` | Pending |
| **Session 81** | Batched | R12 + R107 + R109 + R111(a): loop state and scheduled tasks (CLAUDE.md says none exist; verify, then close or fix); untracked residue (R107(a) adopt-or-delete); a stale `index.lock`; `sync_check` hardcodes `main`. | old Session 59 | Process/Model (P) | `tools/sync_check.py`, `docs/cowork_sync_protocol.md` | `UT test_core.SyncCheckTests` | Pending |
| **Session 82** | Batched | R182 + R186 + R188: the eval harness crash window; a claims archive mode; 1.24 MB of eval residue (the delete waits on Ben's yes, Session 90). | old Session 60 minus R183-R185 | Process/Model (P) | `tools/claim.py`, `skills/generate-lineups-workspace/` | `UT test_core.ClaimSweepTests` | Pending |
| **Session 83** | Batched | R149 + R78 + R44: the installed-skill drift check; environment floors (pandas 3); re-extraction of archived zips (+rider: list misplaced exports). | old Session 61 minus R137 | Process/Model (P) | `tools/audit.py`, `requirements*`, `tools/extract_inbox_zips.py`, `tools/awaiting_standings.py` | `UT test_core.SkillCacheDriftTests test_core.InboxZipTests` | Pending |
| **Session 84** | Batched | R198/R199 + R200 + R30/R38 + R187/R196: mine cost and registry; three archival mechanisms that never ran; the money-backfill miner gap (+riders: entry-fee fallback, cash vs ticket); archetype override. | old Session 62 | Process/Model (P) | `FM`, `tools/rebuild_registry.py`, `tools/net_to_date.py`, `tools/ownership_pred.py`, `data/reference/dk_contest_archetypes.csv` | `UT test_core.ArchiveToolingTests` | Pending |
| **Session 85** | Standalone | R301 remainder + R183/R184/R185: the instruction corpus. Resolve the eleven contradictions, bring SKILL.md to 150 lines plus `references/`, and set the rest of the queue shape. Needs 01. | was CC-40; R183-R185 (old Session 60, absorbed by R301) | Process/Model (P) | `skills/generate-lineups/SKILL.md` and `references/`, `docs/`, `MLB_Classic.md` | `python -m pytest tests/test_upload_integrity.py -k PreflightContractDocumentation -q`; `UT test_core.RootContractBudgetTests` | Pending |
| **Session 86** | Batched | R380 + R376, hooks: R378's findings fallback misses a background subagent's report; the command guard denies a command for describing a banned act. | both were unslotted | Process/Model (P) | `.claude/hooks/subagent_record.py`, `.claude/hooks/guard_commands.py` | `UT test_core.SubagentRecordTests test_core.GuardCommandsTests` | Pending |

### ARCHIVE role (not a DEV session)

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 88** | Batched | Ledger inbox sweep + R1c-tail. <br>• Merge the unconsumed hand-written ledger fragments: 2026-08-28 3-21 five-stack line, 2026-09-02 gate pins 1659 and 1675, 2026-09-06 quick-card pin, 2026-09-18 1905_13g. <br>• Delete the consumed ones named by `_CONSUMED_2026-08-22_DO_NOT_REMERGE.md` and the board. <br>• Process the miner blocks per `docs/cowork_archival_runbook.md`. <br>• Write the nine archetype rows from the retained `2026-08-09_DEV_ben-decision-curated-satellite-archetypes.md`, which then retires. | ledger/inbox (18 hand-written, marker, 343 miner); R1c-tail | Process/Model (P) | `ledger/MLB_Classic_Calibration_Ledger.md`, `ledger/inbox/`, `data/reference/dk_contest_archetypes.csv`, `docs/backlog_inbox/` | `python .claude/hooks/session_start.py --compact` prints `ledger 0` and no retained backlog fragment | Pending |

### Deferred pools (an item moves to a Pending row when its trigger fires)

| Session ID | Packaging Type | Work Unit & Scope | Source Origin | Gate Classification | Target Files | Verification Command / Breakpoint | Status |
|---|---|---|---|---|---|---|---|
| **Session 90** | Batched | Awaiting Ben, strategy decisions: <br>• R41 (Showdown under the three gates) <br>• R206 (controls count entries, not dollars) <br>• R262 + R13 (scenario-selection switch; the stakes decision) <br>• R240 (punt-captain template) <br>• R139 (captain-leverage tiers) <br>• R40 (one-seat satellite routing) <br>• R150 and the R333/R343 cap values <br>• R37(2)(a) floor scope <br>• R25-tail (late-swap fast path) <br>• R188's delete <br>• R273 (dead-code quarantine) <br>• R131(a) (Solo Shot row) <br>• R31(d) <br>• D2 (supersatellite posture) <br>• D3 (captain ladder defaults) <br>• per-contest captain target <br>• R266 severity <br>• whether the two 2026-09-14 research outputs were intended | old Decisions owed; old Tier 4 | Strategy (S) | none until answered | Trigger: Ben's answer, recorded as one line in the entry | Deferred |
| **Session 91** | Batched | Awaiting Ben, data and access: <br>• the GPP payout re-download (all contest types) <br>• R308 (RotoWire RST% transport) <br>• R337 (byte-exact backup of the 07-18 manifest) <br>• R34-tail (three wrong upload_ready Showdown records) <br>• FanGraphs automation <br>• D1 (salary-leave) <br>• the Quick Card split <br>• R231's sweep grant | old Decisions owed; old Tier 4 | Process/Model (P) | none until answered | Trigger: Ben's answer | Deferred |
| **Session 92** | Batched | Gated on a measurement or a start condition: <br>• R256 (after Session 60's residuals) <br>• R263 remainder (after 44 and 52) <br>• R302 stages A+ (start: R118 built, which is done, and ten deliveries graded) <br>• R334(a)'s 1.77x re-measurement (a new Showdown slate with standings) <br>• R81 (slate fingerprint contract; design pass after 30) <br>• R9b (split corpus) <br>• R281 and R282 (P3 repetition) | old Tier 6; old Decisions owed; R334(a) remainder | Process/Model (P) | none until triggered | Trigger named per item | Deferred |

## Retired and Deprecated Artifacts Log

| Artifact | Disposition (2026-09-22, R385) |
|---|---|
| `docs/backlog.md` `# What do we tackle next`: the `## Execution roadmap` tables CC-A0..CC-40, the Phase 6 paragraph (Sessions 43-63), the Decisions paragraph, the dated DEV narratives | RETIRED as a queue; relocated verbatim under `# Board history` → `## Retired 2026-09-22 (R385)`. Every live item maps to a Session above. |
| `docs/backlog.md` `## Tier 1`-`## Tier 6`, `## Kept as-is`, `## Externally gated`, the tier pull rules | RETIRED; relocated verbatim beside the above. Tier 1 had been internally superseded since 2026-08-24. "Kept as-is" is an audit record, not work. |
| `docs/backlog.md` `## Open tails and items awaiting Ben` | RETIRED as a section. R225-R227 moved to Workstream 7 (Session 42); R61-tail → 68; R1c-tail → 88; R25-tail → 90; R34-tail → 91. The Sync-tail is RETIRED: its own entry says nothing waits on the PAT, and the cloud host resolves secrets from the environment (`docs/hosts.md`). |
| `docs/backlog.md` header ("the single live backlog") | Rewritten: the file is the R-entry register. |
| Stale headings R118, R364, R365 | Collapsed to closed stubs; shipped as CC-6/R384 and CC-A5. R118's declined remainders (the policy A/B half, the FPTS-agreement key, Showdown settlement) stay declined per CHANGELOG 2026-09-22. |
| R290 (CLOSED) and its Classic rung-2 remainder | The remainder becomes R401 (Session 22); R290(b) stays in Session 76. |
| `docs/PROGRESS.md` (generated, R366) | DELETED. `python tools/plan_status.py --print` gives the one-screen view; `--check` is the coverage linter. |
| `docs/backlog_inbox/2026-09-22_BUILD_delivery-record-reads-dest-before-promote.md` | CONSUMED → R387, Session 02; moved to `docs/backlog_inbox/_to_delete/`. |
| `docs/backlog_inbox/2026-09-22_BUILD_odds-paste-blocks-on-postponed-game.md` | CONSUMED → R397, Session 04. |
| `docs/backlog_inbox/2026-09-22_BUILD_f4-grades-against-opener-not-bulk-arm.md` | CONSUMED → R398, Session 32. |
| `docs/backlog_inbox/2026-09-22_BUILD_showdown-1915-small-defects.md` | CONSUMED → R399: (a)(e) Session 04, (b) Session 33, (c)(d) Session 63. |
| `docs/backlog_inbox/2026-09-22_BUILD_1905-autobuild-sliced-read-and-untagged-refusals.md` (PR #27) | CONSUMED: §1 → R285 rider (Session 18); §2 → R403(a) (Session 02); sightings → R204 and R98 riders; the `--help` crash is already R399(a). |
| `docs/backlog_inbox/2026-09-22_BUILD_1905-late-swap-needs-feed-dk-starting-ignored.md` (PR #27) | CONSUMED: §1-2 → R404 (Session 28); §3 → R403(b) (Session 02). |
| `docs/backlog_inbox/2026-09-04_BUILD_fangraphs-platoon-refresh-procedure.md` | RETAINED by design until Session 36 ships R317's transport by capture. |
| `docs/backlog_inbox/2026-08-09_DEV_ben-decision-curated-satellite-archetypes.md` | RETAINED by design until Session 88 writes R1c-tail's rows. |
| `ledger/inbox/` (18 hand-written, the `_CONSUMED` marker, 343 miner blocks) | ARCHIVE-owned; Session 88. |
| `.audit/NEXT_IMPLEMENTATION_RUN.md` | Banner repointed from "What do we tackle next" to this file; already stale as a queue since 2026-08-15. |
| `.audit/AUDIT.md` (2026-08-14 edge audit) | Historical; its findings were adjudicated into R-entries on the board. No action. |
| `docs/2026-07-24_session_handoff.md`, `docs/2026-07-25_session_note.md`, `docs/2026-07-25_red_team_final.md` | SUPERSEDED banner. They carry July open-work lists (I-/E- ids) superseded by backlog v2 on 2026-07-27; the last still called itself "the single live backlog". |
| `docs/legacy/MLB_Classic_Backlog.md` | Banner repointed from `docs/2026-07-27_backlog_v2.md` (renamed at R127) to this file. |
| `docs/greenfield/2026-09-19/DFS_SYSTEM_GREENFIELD_SPEC_2026-09-19.md` §1.4-1.6 | Absorbed: F3 and `sha256_file` → R400 (Session 74); `tail_candidate_scanner` → R280 (Session 75); the `csvio` Excluded reader → R302 stage A (Session 92); R297(c) → Session 08; R317 → Sessions 31 and 36. R354's attachment-root question is RETIRED as answered: this session's upload arrived at `/root/.claude/uploads/<session>/`, which `tools/stage_slate.py` ATTACHMENT_ROOTS (L105-111) already searches at depth 2. |
| Twelfth-edition items rejected and filed nowhere: F32, F36, stage E (the DK submission adapter, forbidden by the DK wall), stage F (other sports) | Recorded as rejected so they are not re-raised. |
| `docs/2026-09-22_deadline_delivery_qa.md` (the audit, committed verbatim) | SOURCE, not a backlog. DD-01..10 and packages A-F absorbed per the triage table. No item is marked complete on the report's evidence. |
| `/root/.claude/plans/*.md` (this consolidation's container-local plan) | Superseded on landing; nothing outside the repo is authoritative. |
| `THE_ODDS_API_KEY`, the-odds-api/odds-api.io callers, the odds leg of `tools/fetch_slate_bundle.py`, in-build Open-Meteo and statsapi fetches | DEPRECATED (R402, Ben 2026-09-22). They are replaced by session captures plus offline parsers (Sessions 34-36) and removed in Session 37 after two BUILD slates run on captures. |
| `_to_delete/` dirs, `tools/_scratch_*` | None exist in this clone. Consumed fragments still route to `docs/backlog_inbox/_to_delete/` (gitignored). |

## Living Changelog and Progress Ledger

One row per landing, newest first. `CHANGELOG.md` holds the full entry; this is its index. The next session backfills the SHA of the MERGE that landed the row: `git log -1 --format=%h --no-merges --grep=^R<num> --grep='^Session <NN>:' origin/main` gives the commit (git ORs the two patterns; a batched landing's subject can lead with its Session ID, and Session 03's did), and the last line of `git log --format=%h --ancestry-path --merges <commit>..origin/main` is the merge (R410).

| Date | Session ID | Commit SHA | Completed Scope | Remaining Items |
|---|---|---|---|---|
| 2026-09-25 | Session 108 | (backfill) | R421, filed and landed in one commit: the instruction surface audited against the current Claude model. The delivery-record commit names its paths (the guard stopped it); "Certification fails. Do not hand over the file" follows delivery-first; the gated-suite list reads `EXPECTED_SUITE_COUNTS`; file-diff and history prose across the BUILD skill, references, rules, DEV skills, `MLB_Classic.md` and CLAUDE.md is rewritten to the present rule (class grep 53 -> 7, none in class); `/dev-session` step 6 drops the per-entry subagent mandate; `/land` and `/ship` are model-invocable. Session 11 backfilled with its PR merge | L1, L2, L3, L4, L7, L9, L10 declined with reasons (CHANGELOG) |
| 2026-09-24 | Session 11 | 095e21b | R389(b), baseline-first Classic: `run_classic` publishes an entry-mapped `review_grade_baseline` file (`DKEntries_<tag>_BASELINE_<run>.csv`, its own manifest lineage) before any research, re-read on its exact bytes and kept in `_LAST_USABLE`; a crash in research or the enhanced solve now exits 7 with it (exit 1 and no file at 88aacc6); the enhanced file records beside it and both pass preflight on their bytes; refusals keep 3 and 10 and carry it as the current file; short, refused and errored baselines are named records and the build runs as before; late swap, re-promotion, preflight and outcome review follow the lineage; `resolve_leverage` moved above the call; the probe stays. Session 10 backfilled | R389(c) (Session 12); R419 (Session 106), R420 (Session 107) filed |
| 2026-09-24 | Session 10 | 88aacc6 | R389(a), the baseline core, not wired in: `mlb_engine/pipeline/baseline.py` `build_baseline` puts an entry-mapped, per-contest-distinct candidate set in hand on the unenriched frame, the probe as candidate #1, then one in-memory grid pass (`_MemoryBankCache`, no path, no save) and distinct fill solves (differ by at least one player), per-contest distinct by player set and never over a held row; a spent window returns a typed short count, never a blank row or a raise; it writes nothing (audit-hook test); `unenriched_frame` is BS's ValueError-branch frame, byte-identical before and after; `allocator_candidates` shapes the payload through BankCache's own `as_candidates`. 06-03 covered 7/7/4 in 1.3s, 06-28 blanked covered 38 rows (largest 20) in 10.6s, both twice, same set. a contest named twice, an entry in two contests and a frame off the salary file raise (the diff review's F-3 hole); `BaselineCoreTests` 34, 42 mutations red; golden unmoved; EP untouched, no gap knob (none exists). Session 09 backfilled | R389(b) (Session 11), (c) (Session 12) |
| 2026-09-24 | Session 09 | b14a946 | R388(d) + R124(a) + R268, the review-grade lifecycle. (a) `classify_export_failures` places every failing check by `gate_classes` (S/P placed; a MIXED gate with a V fact unplaced, so V; attribution total), and an S/P-only, essential-valid refusal carries `review_grade_export`: `passed`, `errors[]` and the blocked run unchanged, nothing in `final/`, no promotion. `run_slate` mirrors it as `outputs/<date>/DKEntries_<tag>_UNCERTIFIED_<run>.csv`, labelled `review_grade_uncertified` with `failing_gates`, never over a live gates-passing row; preflight says `review_ready`, never `upload_ready`; build_slate re-reads and presents it on the refusal. (b) R268: a swap finds its parent by the file's bytes (`resolve_parent_run`), before the bank, and inherits the parent's recorded `portfolio_controls` (`--rederive-controls`); its label is never better than the parent's. Reproduced before and after; the real `late_swap.py` swapped off an UNCERTIFIED file with no `--allow-parent-mismatch`. | R388(c) (Session 13) splits the MIXED gates this places as V. R124(b) -> Session 21, (c) -> 67. Mirroring an S/P-only swap is a rider on R414 (Session 101). |
| 2026-09-24 | Session 103 | 2e579cb | R416: on the direct door `run_slate` hands its auto-bank back (`auto_bank_out`) and solves on it again (`reuse_auto_bank`, its record in `candidate_bank`); `build_slate`'s R407 and deadline re-solves reuse solve 1's bank, a solve that builds takes its budget from the window left at that call, and `solve.solves` / the refusal's `solves` / the refusal record carry each solve's bank budget, bank and sleeve seconds and wall, printed as `SOLVE n:` as each lands; 06-28 11g at `--max-seconds 105` 220.1s -> 110.5s; golden unmoved; Session 102 backfilled | R417 (Session 104), R418 (Session 105) |
| 2026-09-24 | Session 102 | 0bbd936 | R415, filed and landed in one commit: the sliced bank's cap is `repo_env.bank_max_candidates` (12/entry at 130s, scaled with the call budget, ceiling 1,536 measured on 06-28 11g), `--bank-max-candidates` with provenance, `bank_stop_reason` on brief and refusal, no exit-10 loop at the cap, autobuild `raise_bank_cap`/`bank_at_ceiling` and a direct-door switch; autobuild's refusal-path grow_bank (dead in production) reads `bank_exploration`; R416 filed | Session 103 (R416) Pending; R204's cost-curve half stays in Session 16 |
| 2026-09-23 | Session 08 | 2eb9267 | R393(b): a run result carries `last_usable_artifact` (path, sha256, the exact bytes, coverage off the bytes, essential validity, a label from the export's own certification), attached to a certified, essential-valid export; Session 08's essential-valid is every post-export gate `gate_classes` gives a V class or fact, caps excluded, with a `preflight` seam for Session 13; `build_slate` presents the file before any narrative and exits 7 ("delivered a prior valid artifact after a later failure") on a crashed promotion, a raising brief or report, a failed mirror or manifest record; autobuild takes 7 as a delivery it never re-runs; `build_asserted` exits through the recording door; a failed mirror moves 0 -> 7 (approved). R297(c), closing R297: `execute_portfolio` and the `validate_only` door end a crash `blocked`/`crashed`, a crash after certification keeps the certified record and hands the export back, and `build_slate` and `late_swap` keep a crash a crash (`raise_on_engine_crash`, its stack kept). Eleven non-blocking `/code-review` findings fixed, each tested. R414 filed (Session 101); Showdown's dead rung 2 noted on R401. Sessions 07 and 100 backfilled | R393(a) (Session 23); R414 (Session 101) |
| 2026-09-23 | Session 07 | 1c82928 | R207 (+R204 naming half): on the interaction refusal the allocator's probe drops each active control alone and names the ones that restore feasibility with their smallest step (`interaction_probe`); failing feasibility checks carry `remedy_typed`; every refusal stamp carries `refusal_validity`; the Classic refusal carries `refusal_remedy`; autobuild reads the typed floor and copies the remedies into its stops; `errors[]` byte-identical, golden unmoved. R396(b): exit 5 off the child contract; exhaustion and spent-resume stop records. R345: autobuild `--declare-pitcher`, recorded as an operator input and restored on `--resume`. R125(a)'s refusal text declined to Session 32 (LDA). Session 06 backfilled | R204 ceiling half (Session 16); R125(a) (Session 32) |
| 2026-09-23 | Session 100 | 1c82928 | R413, filed and landed in one commit: `plan_status.py` parses a two- or three-digit Session ID in the master table, NEXT and the Progress Ledger, and sorts rows by number; a three-digit row used to be unparsed (status unlinted, absent from `--print`) and a three-digit NEXT failed the gate; `PlanStatusTests` +4; the roadmap's IDs line says the width. Rides the Session 07 branch; its gate is that landing's | none |
| 2026-09-23 | Session 06 | 1eb592c | R388(b): every resolved Classic control carries its provenance in `control_provenance` (`engine_default`, `posture_default`, `derived_floor`, `weather_derived`, `confidence_derived`, `operator_relaxable`, `operator_never_relax`; per game for the per-game dict), the vocabulary and F-3's fact authority in `gate_classes.py`; `--never-relax` on BS, AB and LS holds a control at the rung (`held`), the merge, the pipeline's and the swap's floors, R407's tightening and autobuild's structural floors, and refuses at exit 4 a name the allocator's own ladders, its sleeve fallback or Showdown's R153 ladder would still move; the rung records `moves` (before, after, provenance, reason) and EP no longer calls its values the operator's; a typed cap alone stays relaxable; the DK-rejection comment and three stale per-game comments corrected; golden unmoved; Session 05 backfilled | R388(c) (Session 13), (d) (Session 09); the refused never-relax names ride R391(a) (Session 19) and (b) (Session 20) |
| 2026-09-23 | Session 05 | ed18ed7 | R388(a): `mlb_engine/entries/gate_classes.py` holds the governor's gate table (moved verbatim) beside a V/S/P class for 21 gate names and all 11 refusal sites, MIXED checks split into named facts, four evidence states; the V/S/P lookups raise on an unclassified name, the governor's keeps its READ-IT default; BS reads it lazily, DG imports the four strings; no behaviour change, golden unmoved; the "never fires" default found reachable by three names; Sessions 87 and 04 backfilled | R388(b) (Session 06), (c) (Session 13), (d) (Session 09) |
| 2026-09-23 | Session 04 | 2864fb7 | R399(a)(e): `BS --help` exits 0 (`100%%`); the `--captain-sleeve` help, `captain_sleeve.label` and the block's missing `selector` follow R382. R397: `paste_odds` exempts a game a `--feed` marks postponed, cancelled or suspended or `--exclude-game` names, reported `excluded_postponed`, its priced rows dropped; DK's literal named. R170: the preserve fallback keeps the file's own tag; a named missing `--odds` is recorded (`odds_file_missing`) and never replaced by a fetch; `--bundle` the same. Evals 6 of 8 (R411's two), unchanged | R399(b) (Session 33), (c)(d) (Session 63); this row's and Session 87's SHAs are one merge, backfilled next session |
| 2026-09-23 | Session 87 | 2864fb7 | R412 filed: the Showdown melt's same-name refusal scoped per person rather than per key (PR #18's per-key scoping measured to drop a healthy twin), and the brief's `captain_budget.hold_yielded_to_lock`; Sessions 98 and 03 SHAs backfilled (`fd8673a`); this table's backfill command also greps a `Session NN:` subject | Session 87 Pending; NEXT stays Session 04 |
| 2026-09-23 | Session 03 | fd8673a | R388(e): a governed Classic retry records `review_grade_deadline_build` in `run_slate`, so brief status, manifest row, delivery record and preflight (`review_ready`) agree; an accepted late-swap downgrade records `review_grade_downgrade_accepted`; a re-promotion keeps both. R298: the manifest reads the allocator's three ladders; the Classic brief carries `manifest_recorded`, `mirror_error`, `delivered_sha256_error` and `_unmirrored`; `find_prior_file` refuses another slate's lone file; retro's `manifest_recorded_utc`. R396(a): exit notes 3 and 4. R377: the Classic and re-promotion records carry their controls. R411 filed (Session 99) | R388(a)-(d) (Sessions 05, 06, 13, 09); R396(b) (Session 07); R411 (Session 99) |
| 2026-09-23 | Session 98 | fd8673a | R410: the gate is described once per host. The BUILD skill's Session hygiene gives the one-call gate and points at `docs/hosts.md` and `docs/cowork_sandbox.md` (net -8 lines); `/dev-session`, CLAUDE.md's Hosts bullet and this file's Sizing line point at `docs/hosts.md` rather than carry a time; `.claude/rules/skills.md`'s line count and two stale "40 modules" corrected; this table's backfill command returns the merge; `HostProseIsCurrentTests` +1; Session 97's SHA backfilled | none |
| 2026-09-23 | Session 97 | 648f8cf | R409: `/dev-session` gets a finish line, a stop list, a `dfs-premise` fan-out with the parent re-running the evidence, and a closing report order; CLAUDE.md says a progress report is not a stop; `claims/<claim>/TASKS.md` is re-injected by PreCompact (held claims only, an earlier holder's list rotated aside by `take`); `/land` gets a `/code-review` pass; four stale push-is-Ben's sites fixed | `disable-model-invocation` on `/land` and `/ship` makes both a stop for Ben to type; dropping it was denied as self-modification and is Ben's call |
| 2026-09-23 | Session 94 | afb833e | R406: Classic scenario sleeves (projection, salary-only, chalk-fails, environment) at Ben's weights, built on all three bank doors through one helper and seated through the allocator's mask with every cap joint; environment is a membership and drops on a two-game slate; production golden re-frozen | Sessions 93-96 complete; NEXT stays Session 03; the 1905_10g replay for R405 is owed when its files are staged |
| 2026-09-23 | Session 96 | 1e1a01d | R408: `tools/replay_slate.py --grade-projection` grades engine Base, Ceiling, salary and APPG against realized points on 9 archived Classic slates (7 archive, 2 fixtures; premise corrected from 5), per slate and side, never pooled; ARCHIVE note filed as a ledger fragment | Session 94 next in Ben's order; NEXT stays Session 03 |
| 2026-09-23 | Session 95 | 1e3311d | R407: a deterministic input-confidence tier from the brief's four degradation facts tightens `max_player_exposure_pct` and `max_consensus_cluster_share_pct` in `run_slate` (DEGRADED -0.05/-0.10, SEVERE -0.10/-0.20), floors and overrides win, relaxed first inside T-30 and on a proven-infeasible MILP; golden unmoved (its door supplies no facts) | Sessions 96, 94 next in Ben's order; NEXT stays Session 03 |
| 2026-09-23 | Session 93 | afbaf68 | R405, all three parts: the bank's consensus cluster reported on the brief and in QA, capped by `max_consensus_cluster_share_pct` (0.50 on the multi-entry postures, k = 3) in an R343-shaped row, and supplied by cluster-limited bank jobs on the sliced, direct and plan-leg doors; production golden re-frozen (18/18 -> 9/18 at 3+, 11 -> 18 distinct lineups) | Sessions 95, 96, 94 next in Ben's order; NEXT stays Session 03; the 1905_10g replay is owed when its files are staged |
| 2026-09-23 | Session 93 | 0c2ea3c | R405-R408 filed (Sessions 93-96) from Ben's 1905_10g post-slate review; R247(d) answered and moved out of Session 90; Session 02 SHA backfilled | Sessions 93-96 Pending; designs, Ben's defaults and build maps in the riders |
| 2026-09-23 | Session 02 | 9532367 | R387: record rosters parsed from the hashed provisional bytes on all three writers, sha256-bound, a mismatch recorded not written; R377 (partial): Showdown and late swap pass controls and relaxations, egress from the session-start reading; R403: refusal records carry tag, class, errors[:3] and failing checks, run-less records keyed `<tag>_norun_<sha12>` | R377's Classic controls → Session 03; Sessions 03-88 Pending; 90-92 Deferred |
| 2026-09-22 | Session 01 | 93a7c1b | R386: V/S/P defined in MLB_Classic.md §2; CLAUDE.md Autonomy and T-schedule give S relaxation and P recording to the session inside T-30, V stays a wall; gate and solver probe skipped inside T-30; `--accept-downgrade` on an authorized repair is the session's; F-1..F-4 and R125(b)(c) recorded; the skill mirrors them | Sessions 02-88 Pending; 90-92 Deferred |
| 2026-09-22 | Session 00 | 786e73d | R385: one roadmap; R386-R404 filed; six fragments consumed; queue sections retired; PROGRESS.md replaced by the linter; Ben's F-1..F-4 recorded | Sessions 01-88 Pending; 90-92 Deferred |
