# DFS System Greenfield Spec, Seventh Edition

**Review date:** 2026-08-24. **Reviewed base:** `main` at `a49bd610` ("R167 + R168 + R169: the lost-window batch"), 28 commits and ~14.4k inserted lines past ed6's review base `ac8ac05`. HEAD did not move during the review (re-checked by every lane at start).
**Repository:** the live mount, `C:\Users\benja\Documents\Claude\mlb-dfs`. Disk and GitHub agreed at `a49bd610` at session start (`sync_check`, GH_PAT leg, "ok disk and GitHub agree"); working-tree dirt was 4 ARCHIVE-owned files plus untracked archive/inbox material, left alone.
**Suite evidence, before any review:** `PASS  v2.26.0  26 modules  1276 tests` via `tools/audit.py --gate-report --terse`, the complete chunked-gate state left by this morning's DEV session and validated by the gate's own tree fingerprint. No suite off its pin, no skips reported on the line.
**Mandate:** Ben's greenfield prompt (assume nothing correct; line-by-line review; zero-touch maximum-EV target; write the spec). Sixth time this prompt has run; the adjudication record of editions 1 through 6 (docs/2026-08-0{1,10,12,17,22}_critique_*.md, backlog "Do not build", CHANGELOG) was read in full FIRST, so nothing below re-argues a standing rejection or re-files R1..R211.
**Filename note:** the requested name `DFS_SYSTEM_GREENFIELD_SPEC.md` is occupied by the tracked third edition, which the adjudication protocol keeps as the diff base (ed4's filename note, unchanged). This file follows the ed5/ed6 convention: dated, in docs/, beside the prior editions.
**Method:** five parallel reviewer lanes (post-ed6 tools diff; post-ed6 engine diff; open-queue premise re-verification; ed6's published cut list; test assertion strength), plus a coordinator pass that re-read every headline citation against the tree before filing. Reproduce-before-filing was enforced until the sandbox bridge died mid-review (see Method appendix): repros that ran before the outage are quoted; everything after carries VERIFIED-read or PLAUSIBLE labels honestly.
**Walls, held throughout:** DraftKings reads are manual; the money-and-entry wall is absolute; nothing below automates DK. Zero-touch is designed between the two manual clicks, as every adjudicated edition has held. Truthful labels govern this document: nothing here is ROI, win rate, cash rate, or a probability claim; diagnostics are deterministic review proxies and priors are labeled priors.
**Numbering:** findings carry GF7 ids. R-numbers are allocated by the landing DEV session by scanning backlog + CHANGELOG at commit time (current max R211); do not pre-trust any number in this file as a board number.

---

# Section 1: Greenfield System Audit & Legacy Deconstruction

## 1.1 Verdict

Every module keeps, for the seventh consecutive review. Twenty-eight new findings (3 High, 10 Bug, 15 TechDebt/coverage), zero rebuild arguments, Do-not-build untouched, the four-clause sim gate untouched. The rejected-program family (solver migration, artifact store, DAG orchestrator, signed manifests, watcher daemons) did not return because no lane found a measured problem those programs solve.

The defect texture is the record's own doctrine confirmed a sixth time: **new code is where the defects live, and the seam of a fresh fix is the newest code of all.** Seventeen of twenty-eight findings sit either in code landed in the last seven days (the R152/R153/R154/R159/R167-169 surfaces) or in the unenumerated remainder of a class those fixes closed. Eleven sit in surfaces ed6's spend limit cut and no edition had read (miner aggregation, contest library, legacy allocator half, slate-clock internals, promote path). The pre-existing core held again: the verified-clean census in Section 2 lists what was checked and did not move.

## 1.2 First-principles finding: the fix class itself recurs, and it is enumerable

Four of this edition's sharpest findings are the same event: a fix that closed N named sites of a defect class shipped while the class had N+1 members.

- R167 unified the fraction-cap units rule across "four copies", and the fifth fraction control (`max_game_exposure_pct_by_game`) still silently clamps at three sites and is absent from both refusal boundaries (GF7-T3). The gate the fix built also passes NaN and bool (GF7-E2).
- R169(a) stopped the TimeoutExpired path from destroying the decision log, and the sibling wall-clock stop eight lines above it still destroys it (GF7-T2).
- R153 made both Showdown caps "bind on every rung", and the ladder's floor rung still drops the captain cap with no counter (GF7-E3), on a path no test drives.
- R159 unified "two components answering one question differently" for coverage-vs-merge, and minted a new disagreement between its own comment and its own guard: a degraded DK side defers to ANY nonempty feed lineup, not to a complete nine (GF7-E1, repro'd).

The board already carries the right half-sentence ("when an item says the Nth copy, count the copies before believing N", 2026-08-24 queue note). The missing half is mechanical: **a landing that claims "the rule now lives in one place" should include, in its CHANGELOG entry, the grep pattern and hit list that enumerate the class at that head.** The R167 entry narrates four copies; a `grep -n "min(1.0" | floor(total *` style enumeration would have surfaced `_game_cap_count` and forced the "deliberately kept" rationale to answer the silent-disable harm, not only the solve/validate-agreement harm. One sentence of DEV discipline, no code, and it converts this edition's most repeated finding shape into a diff-time check.

## 1.3 Context and prompt economics

Status, not re-argument: CLAUDE.md is 33,975 bytes today (unchanged since the ed6 landing measured the same number; the R183 curve has at least flattened this week), SKILL.md 52,192B, MLB_Classic.md 49,179B, backlog 5,059 lines with the R184 relocation holding. The Quick Card pin line has absorbed two more dated corrections and remains one physical line; the Tier 4 split decision is unchanged and its price was already measured at 2.3x the original estimate. Nothing new to file; R183/R184/R185 and the Quick Card split remain the owned fixes. The chunked gate paid for itself this session: session-start evidence cost one `--gate-report` call because the prior session's complete state was fingerprint-valid.

## 1.4 LLM vs deterministic boundary

Unchanged and correct: parsing, joins, legality, feasibility, solver work, exposure math, hashing are deterministic and already are; pastes, orchestration, review synthesis stay at the edges. The two standing boundary gaps are unchanged (R121: `tools/slate_clock.py` still does not exist, re-confirmed; R203: autobuild's docstring still refuses the exposure-cap rescue Ben delegated on 2026-08-22). This edition adds one datum to the R203 case: the supervisor currently cannot even PRESERVE an operator's R157 rescue, because its own `--controls-override` replaces the passthrough's (GF7-T5). The manual workaround the delegation depends on is the thing the supervisor discards.

## 1.5 Zero-touch boundary

Zero-touch remains: everything between (click 1) Ben dropping DKSalaries/DKEntries and (click 2) Ben uploading the certified file. The distance to it, measured at this head, is exactly: the queue's own slots 1-9 (all premises re-verified current, Section 2.5), R121, R203+R207, and the four lost-window doors this edition adds (GF7-T2, T4, T8, E2). Nothing else on the zero-touch path is open. Outside the two clicks, automation stays refused by the walls, and this edition, like ed5 and ed6, treats the prompt's request for scraped DK downloads and auto-submission as answered by design: DK's terms prohibit it, the project's rule is stricter, and the two clicks are constraints, not friction.

---

# Section 2: Comprehensive Code Review & Defect Log

Verification labels: VERIFIED-repro (script ran, output quoted), VERIFIED-read (exact lines quoted; coordinator re-read the headliners independently), PLAUSIBLE (mechanism read, not executed). Line numbers are against `a49bd610`.

## 2.1 High findings

**[High | GF7-T1] tools/audit.py:1016 -- the gate's tree fingerprint omits skills/, and ~20 gated tests exec `skills/generate-lineups/scripts/build_slate.py`.**
```python
for folder in ("mlb_engine", "tools", "tests"):
```
against test_core.py:4212 `spec_from_file_location("build_slate_under_test", path)` (also :7291, :12790, and the R167-169 batch's new units/exit-contract tests), plus test_upload_integrity reads of the same file. R152's honesty rule 3 ("every record carries a fingerprint of the tree it ran against, and the report refuses a mixed set", :937-939) is therefore false for the one 3,325-line file that changed three times this week: edit build_slate.py between `--gate-run` calls and the fingerprint does not move, no reset fires, and `--gate-report` prints the byte-pinned clean line over records whose build_slate assertions ran against the previous file. The single-call `--run-tests` path is immune; the split path, which is the documented Cowork path, is the exposed one. First bad moment: a DEV session lands a build_slate edit mid-gate and quotes a green line that never tested it. VERIFIED-read (coordinator re-read both halves). Fix: add `"skills"` to the folder tuple (the rglob already skips `__pycache__` and `_scratch_*`); accept the one-time reset of in-flight gate state.

**[High | GF7-T2] tools/autobuild.py:189-191 -- the wall-clock stop drops the entire decision log; R169(a)'s exact defect on the sibling exit.**
```python
if time.monotonic() > deadline:
    dec.add(attempt, "stop", "supervised wall clock spent")
    return 5
```
Every other exit in `main()` flushes via `_write(dec, ...)` (:241 timeout, :255, :274, :306, :312, :324, :337, :340); this one returns directly. Reachable by construction with defaults: 8 attempts x up to `per_build_seconds+90` against a 12-minute wall clock, so a run that grew the bank five times and ran out of window files NO `autobuild_decisions.json` at all, on exactly the run whose post-mortem needs it. Three independent reads agree (tools lane, tests lane census, coordinator), and the tests lane confirmed no test matches "wall clock": the seven new SupervisorHardeningTests drive TimeoutExpired and never this. VERIFIED-read. Fix: `_write(dec, last_brief, salary=a.salary)` before the return; the test is exit 5 AND `len(logs) == 1` with `time.monotonic` patched past the deadline.

**[High | GF7-E1] mlb_engine/intake/live_data_adapters.py:685 -- a degraded DK side defers to ANY nonempty feed lineup, and the pool report then asserts a seeding that never happened.**
The R159(a) comment (:676-679) states the rule: "DK's eight are seeded ONLY where nothing else has the side: a source holding a complete nine outranks eight-of-nine." The guard is:
```python
side = dict(game.get(key) or {})
if side.get("lineup"):
    ...
    continue
```
Any nonempty lineup wins, including a 1-to-3-hitter mid-repost API partial. Both routes end "partial", so R60 seeds 3 posted starters instead of DK's 8 observed, Player_ID-keyed slots, and fills five extra seats from priors. Compounding it, `build_slate_pool` (:1441-1445) warns "the surviving N are seeded as a partial" off `degraded_sides` without recording whether the merge seeded or deferred, so the report claims a seeding that did not happen (and fires wrongly even in the correct-deferral case). First bad moment is R159's own expensive moment: a Status flip inside a posted nine while the feed holds a mid-repost partial for the same side. VERIFIED-repro (pre-outage; synthetic 9-row NYY salary map, slot 5 IL, 3-hitter feed partial): `feed side kept rows: 3 ... degraded_sides: [{"team": "NYY", "posted": 8, ...}]`. Coordinator re-read the guard. Fix: defer only on `len(side.get("lineup") or []) >= DK_ORDER_SLOTS` or `lineup_status == "confirmed"`; add `seeded` vs `deferred_to_feed` to each `degraded_sides` record and branch the pool warning on it. This is zero-day R159 code; land it with slot 1's neighbors.

## 2.2 Bug findings

**[Bug | GF7-T3] The sixth fraction control skips both R167 boundaries and still silently clamps.** `max_game_exposure_pct_by_game` is absent from `FRACTION_CONTROL_KEYS` (build_slate.py:123-129, coordinator-confirmed) and from `_merged_controls_for_build`'s `_fraction_control_keys` (execution_pipeline.py:2220), while all three `_game_cap_count` sites clamp: `min(1.0, max(0.0, float(pct)))` (contest_allocator.py:2544, :911; dk_entries_manager.py:767). A per-game units slip (`{"401234": 40}` for 0.40) passes the zero-cost gate, passes the merge, caps nobody in the solve AND in the post-export validator, with no counter and no warning. That is verbatim the harm `assert_fraction_cap`'s own docstring names. The R167 entry kept the clamp because solve and validator agree; agreement does not answer silent-disable. late_swap steers operators to override exactly this control (late_swap.py:197). VERIFIED-read. Fix: route each by-game value through `assert_fraction_cap` at the merge; add the dict-valued key to build_slate's gate; keep `pct <= 0` semantics.

**[Bug | GF7-T4] build_slate.py:3219, :3261 -- two unguarded reads in `main()` are exit-1 crash doors of R168's class.** `json.loads(Path(args.lineups).read_text())` (mistyped path: FileNotFoundError) and the staged-feed read (torn `lineups_feed.json` from a killed call: JSONDecodeError). The R168(b) AST test pins only `fetch_lineups` calls. Downstream, autobuild logs "refused with no remedy this supervisor may take, errors=[]": a crash wearing a refusal's label. VERIFIED-read. Fix: bad `--lineups` refuses at exit 4 with a payload; unreadable cache treated as absent, falling to the guarded fetch leg.

**[Bug | GF7-T5] tools/autobuild.py:184, :218-219 -- the supervisor's `--controls-override` replaces, never merges, an operator's passthrough override.** `controls` starts `{}` and gains only structural floors; when appended (`if controls: cmd += ["--controls-override", json.dumps(controls)]`, after the passthrough by R169(d) design), argparse last-wins drops the operator's dict entirely. Attempt 1 honors a passthrough R157 rescue (`{"max_player_exposure_pct": 0.55}`); the first structural floor erases it for every later attempt while the log records only the floors. The R169(d) intent was "supervisor-owned flags win"; the implementation makes the supervisor discard operator values for keys it does not own. First bad moment: the next 1335_3g-shaped rescue silently reverts to posture caps mid-run. VERIFIED-read (coordinator re-read). Fix: seed `controls` from a `--controls-override` found in the passthrough (shlex already tokenizes it), or refuse the combination loudly. Rides R203.

**[Bug | GF7-E2] contest_allocator.py:1035-1036 -- `assert_fraction_cap` passes NaN and bool.** `float("nan") > 1.0` is False, so NaN clears the R167 gate at every boundary and dies later at `math.floor(total * value)` with an unnamed ValueError, after the bank spend: the lost-window class arriving through the gate built to stop it. `assert_fraction_cap(True)` returns 1.0 silently (bool is int), a cap switched off with no name. VERIFIED-read (coordinator re-read). Fix: `if not math.isfinite(value) or value > 1.0: raise`, plus optional bool rejection.

**[Bug | GF7-E3] showdown_theses.py:722-725 -- the ladder's floor rung drops the captain cap with no counter.** Every other rung passes `cpt_excludes=cpt_excludes` (:670, :674, :681, :688, :695, :708); the floor rung's call does not (coordinator-confirmed by kwarg census), and nothing counts it: `cpt_relaxed` counts lock substitutions, and no `cpt_cap_relaxed` exists. R153's "the cap is enforced there now, on every rung" is false at this rung, and the tests lane confirmed the caps-on and floor-rung-forcing fixtures are disjoint by design, so no test can see it. A floor-rung solve may also re-seat the exact captain `cpt_cap_reassigned` claims was removed. The delivered brief reads "0 relaxations" over a breached 25% cap: the washout axis reading clean, which is R153's founding defect. VERIFIED-read. Fix: pass `cpt_excludes` at the floor rung behind a counted `cpt_cap_relaxed` (relax-and-count, ordered after player exposure per the documented order), and drop the reassignment record if the floor solve re-seats that captain; add the missing fixture (floor rung with `cpt_excludes` present).

**[Bug | GF7-T8] The units gate validates but never coerces, so a JSON-quoted number crashes after the solve.** build_slate's gate float-tests a copy and forwards the raw string; `_merged_controls_for_build` asserts then stores the raw (execution_pipeline.py:2281). Classic re-coerces at `_cap_count`; Showdown does not: `"0.5" >= 0.33` raises TypeError in brief assembly and `f"{pct:.0%}"` raises ValueError, both after the full solve. Trigger: `--controls-override '{"max_player_exposure_pct": "0.5"}'`. PLAUSIBLE (path read, not executed). Fix: store `float(value)` back at both boundaries.

**[Bug | GF7-E6] live_data_adapters.py:1464-1469 -- the R160 `games_without_lock_time` blocker is not filtered to the slate.** The sibling `games_unsynthesizable` loop filters on `slate_team_set` (:1453-1454); this one does not, and the postponed-game exclusion runs after the skip that feeds it. A whole-day feed with one malformed off-slate `game_date_utc` blocks a build its pool cannot touch. VERIFIED-read. Fix: apply the same team-set filter.

**[Bug | GF7-E7] live_data_adapters.py:727-730, :690-696 -- degraded-side edges.** (a) `games_unsynthesizable` appends per TEAM, so one DH game 2 with DK data on both sides emits two blockers for one fact. (b) An all-nine-shelved side seeds an empty "partial" (`lineup=[]`, covered, `_note_hands` fires a spurious `f4_handedness_unavailable`, and the pool warning reads "the surviving 0 are seeded"). VERIFIED-read. Fix: dedupe by gid; skip the seed and the hands note when the survivor list is empty.

**[Bug | GF7-E8] live_data_adapters.py:622-674 -- the DK merge stamps the slate leg's order onto every feed leg of a doubleheader and emits wrong-leg disagreements.** The confirmed loop walks the raw feed before leg selection (leg selection runs later in `build_status_map_from_lineups_feed`); on a DH day DK's order merges over the other leg's posted lineup and mints `disagreements` rows sourced from a leg the build then drops. Pool outcome is unaffected (the kept leg is correct); the operator-facing report is not, and CLAUDE.md sends the operator to exactly that report. Ed6's DH-merge finding was dropped at landing as overtaken; this is its surviving narrower mechanism at the merge stage. VERIFIED-read mechanism; PLAUSIBLE occurrence (needs both legs posted, routine on DH days). Fix: leg-select against `_salary_game_times` before the merge loops, or suppress disagreement/upgrade rows for legs whose start mismatches the salary time.

**[Bug | GF7-E9] projection_builder.py (R189(2) block) -- the Savant name-join discards its collision report.** `sp_id_by_name, _ = _build_name_to_mlbam(...)` throws away the collision counts the helper returns for exactly this purpose; an id-less probable whose normalized name collides in the pitching table takes the other pitcher's quality silently, while the xwOBA/xISO joins report their collisions. VERIFIED-read. Fix: keep the second return; mark joined collisions in `sp_quality_name_joined` or route to `sp_quality_unavailable`.

**[Bug | GF7-S1] field_miner.py:770, :1244, :1343 -- Showdown duplication counting is captain-blind at all three sites, and Tier 2's grading substrate inherits it.** `players_norm` is sorted and position-blind by design (:264, with the R39 comment saying so); `dup_groups`, `summarize_own_entries`, and the registry recompute all key on it alone, so two entries with the same six players and different captains count as copies. Every Showdown duplication number in ledger 3.17 that R10's entry cites (27.2% median duplicated share; "winning lineup duplicated 40% of the time"; own 27/109) is inflated by construction. Classic is unaffected; R10's unblocked satellite cell is Classic, which bounds the damage. `captain_norm` already rides every parsed entry, so corrected counts are re-derivable from archived JSONs with no re-mine. VERIFIED-read (coordinator re-read all three sites). Fix: key Showdown groupings on `(players_norm, captain_norm)`; add the one-sentence caveat to ledger 3.17's Showdown rows at the next ARCHIVE pass.

**[Bug | GF7-S7] tools/promote_run.py:169-171 -- the immutability bind is skipped when the run manifest lacks the artifact row.** `recorded_sha = str(recorded.get("sha256") or "")` then `if recorded_sha and ...`: absent evidence promotes with no hash check and no message. Fail-open on missing evidence at the re-promotion path, the R177/F16 class. VERIFIED-read (coordinator re-read). Fix: one WARN naming the unverifiable bind, or refuse without `--force`-class acknowledgment.

**[Bug | GF7-X1] tests/test_core.py:1293 -- the game-exposure solver assert was vacated by R116's reuse default.** `test_game_exposure_cap_solver_and_validator` asserts `chosen == ["X", "Y"]`, an outcome the post-R116 `default_candidate_reuse_cap(2, 2) = 1` now forces with no game-cap rows at all; the suite-wide sweep found no other solver-enforcement pin for game caps. Deleting the game-exposure rows from the MILP survives the suite. VERIFIED-read. Fix: pin the cap on a fixture where reuse does not force the outcome (3+ candidates), or assert the constraint rows exist.

## 2.3 TechDebt and coverage findings

**[TechDebt | GF7-T6] tools/audit.py:1036-1047** -- `.audit_gate/` is claimless shared state; two interleaved `--gate-run` sessions double-record a suite, `ran > pinned` reads `grew`, and the operator is told the pin is stale. Fix: freshness check on `started` records, or name `.audit_gate/` in the multi-session contract.
**[TechDebt | GF7-T7] tools/audit.py:2095-2103** -- `--gate-report --output <path>` never writes the file (the non-gate branch does, :2113-2114).
**[TechDebt | GF7-T9] tools/audit.py:1345 vs :1446** -- `--gate-run` says "complete: run --gate-report" on state the report will refuse for age (`GATE_MAX_AGE_H`); add the age check to gate_run's complete branch.
**[TechDebt | GF7-E4] showdown.py:423-437** -- `player_cap_structural_floor` accepts `n_entries` and never uses it; the un-quantized `roster_size/pool_size` it returns can be insufficient at small n (pool 20, n 19: reports 0.30; floor(0.30x19)=5 covers 100 < 114 seats). R157 sends operators to re-derive targets from these floors. Fix: return the quantized floor or relabel `necessary_lower_bound`.
**[TechDebt | GF7-E5] showdown.py:419** -- `exposure_cap_count` lacks the `+ 1e-9` epsilon all three sibling resolvers carry, and sits outside the copy-agreement test; drift bites overrides, in the stricter direction. Fix: add the epsilon or share the helper.
**[TechDebt | GF7-S2] contest_library.py:185-189** -- `save_registry` is a naked truncate-write with no corrupt-read quarantine; sibling `_write_registry` was hardened for exactly this kill vector. Fix: copy the tmp+replace body; quarantine on decode error.
**[TechDebt | GF7-S3] field_miner.py:1348-1375** -- the opponent registry pools Classic and Showdown per-user construction stats (`max_stack` 6 vs 5 are incommensurable; `dup_entries` inherits GF7-S1). Rides R198's registry rework: split aggregates by `contest_type`.
**[TechDebt | GF7-S4] slate_intake_manager.py:1455, :1496-1500** -- wind-band seams (12.999-13.0, 17.999-18.0) and any sub-8 caller threshold band to `""` and silently no-op while `wind_applies` was True. Fix: contiguous half-open bands or a named no-band note.
**[TechDebt | GF7-S5] slate_intake_manager.py:1686-1687** -- `slate_clock` drops unparseable Game Info rows with no `unparsed_game_info_rows` count; a partial clock reports fewer games than the salary file carries, silently.
**[TechDebt | GF7-S6] contest_allocator.py:1091-1098** -- the legacy `assign_lineups_to_contests` path mints `selection_certified: True, allocation_certified: True` for a solve that never ran ("No contest entries requested"). No production caller (census below); quarantine rationale, not a live defect.
**[TechDebt | GF7-S8] field_miner.py:775-779** -- the contest "winner" is the max-points COMPLETE entry, not the rank-1 row; an unparseable winner lineup silently shifts `winning_points`/`winner_copies` to second place.
**[TechDebt | GF7-X2] test_core.py:2799** -- the value guard's clip arithmetic is pinned once and well (8.64 pins headroom, pool, and pitcher exemption) but `VALUE_GUARD_PCTL` is unpinned DOWNWARD: the fixture's distribution returns the same quantile for every q <= 0.933, so `0.90 -> 0.5` survives the suite. A strategy dial with no assert watching it. Fix: a second fixture with a spread distribution, or assert the emitted `"percentile"` field.
**[TechDebt | GF7-X3] bank membership seam** -- `BankCacheAnsweredJobTests` pins skip-answered (the money behavior) and `clear_attempted` integrity, but every cross-condition fixture changes locks and excludes together, so loosening only the MEMBERSHIP comparison to ignore the conditions-signature component survives: answered jobs would suppress new work after a projection/exclusion change under identical locks (the 2026-07-26 incident class). Fix: one fixture that changes conditions only.
**[TechDebt | GF7-X6] fixture-hygiene candidates (R155 class, none red today):** test_core.py:803-804/:816-817 CWD-relative `data/reference/...` strings escape the R62(a) meta-test (breaks off-root runs); :12227 reads the live platoon reference (an ARCHIVE refresh can redden DEV's suite); :13976 `.env`-gated test skips forever in every clone (inverse-R155, one of the known container skips); test_upload_integrity.py:1591 rglobs the live archive behind a skip guard.
**[TechDebt | GF7-H1] outputs/ test litter: 1,778 `_test_r3_*` directories (2,274 files).** Root cause: test_upload_integrity.py:606 deliberately fixtures inside the real `outputs/` because `preflight_upload.is_delivered_file` resolves against `REPO/outputs` (:815), and tearDown's `unlink` fails open on this no-unlink mount, so every mount-side gate run leaks one dir per test in the class (~154 dirs since 08-20; roughly one gate's worth per day). Contained: `outputs/` is gitignored and both scanners skip `_`-prefixed dirs (awaiting_standings.py:167 names "the R3 leak" explicitly). Fix, two halves: sweep via `mv outputs/_test_r3_* _to_delete/r3_litter_<date>/` (mv works where rm does not); prevent by having tearDown rename into gitignored `.tmp/` when unlink refuses. Do not move the fixture to a tmpdir: the fixture's own comment is right that it would test a different `is_delivered_file` path than production.
**[TechDebt | GF7-H2] Root and outputs/ session residue, inventoried:** root logs `_audit.log`, `audit_out.log`; outputs logs `_chunkA-D.log`, `_paste.err/out`, `_audit_final.log`, `_build_sd1.log`, `_perm_probe.txt`, `_test_core*.log`, `_test_rest*.log` (13 files, ~39KB, no in-repo writer: session shell redirects); root tarballs `_changes.tar.gz`, `_stage_repo_{build,r147,r149}.tar.gz` (the `_stage_repo*` class is gitignored per the 08-12 landing); `upload_hou_sd` is an EMPTY untracked directory (stray from a HOU Showdown session; no board owner); `DKSalaries.csv`/`DKEntries.csv`/`slate_bundle.json` at root are live staging convention; `DFS_GREENFIELD_REVIEW_2026-08-12_ed4.md` remains untracked at root while its adoptions are all landed (a DEV session should `git add` it beside the other editions or mv it to docs/). Disposition for all residue: the `_to_delete/` mv sweep; none of it is hazard-class except as `git status` noise that the fragment-census lesson (08-23) already named.

## 2.4 Board-known items confirmed at HEAD, and board corrections

Queue slots 1-9 all HOLD at `a49bd610`; none is overtaken. Verified with repro where cheap (pre-outage): **R205** (synthetic ATL@MIN books through `parse_the_odds_api_totals`: `moneyline {'ATL': -106.0, 'MIN': -2.0}`, the filed numbers exactly), **R172** (`guard-only -> enriched, empty -> proxy`), **R165** (int64 frame: string excludes no-op in both helpers), **R181** (eval 5 forbidden token vs canonical vocabulary, both greps). By read: R191, R192, R173 (evidence string now reads "under 5 hitters", R133 rewording, premise intact), R176 all four legs (with the (c) location correction below), R163 (:2501-2505), R174, R175, R179, R180 (lines moved), R164 all three legs, R158 (:360-361), R189(3), R122 rider (`prior_note` :831-833 still claims the full chain; zero `Pool_Basis` reads in showdown_theses), R121, R166 (all three legs, `total = E + fixed_rows` at :2179 vs solve-entries-only constraint at :2452), R197 (`Ownership_Tier == 'Low'` at optimizer_v3.py:3043, no writer; R154's parallel pct-based floor sharpens, does not moot), R203/R207 (docstring wall intact), R211, R208, R194, R71(c), R177, R83 rider, R186.

**Board corrections for the next DEV pass (line/wording deltas, verified):**
- **R191:** the auto-resolve is a POINTER READ of `runs/latest_valid_run.json` (preflight_upload.py:358-388), not an mtime scan of runs/; the function never sees the entries file, which makes the fix a scope-or-refuse inside one 30-line function. "Newest on disk" in the What should say so.
- **R205:** the code is `statistics.median(sorted(...))` (live_data_adapters.py:2271), which on the two-book production fetch degenerates to the arithmetic mean; the entry's "median with a sign guard" fallback option is therefore not a fix and should be struck. Probability-space averaging (or single-book) survives any book count.
- **R202 is PARTIALLY OVERTAKEN and needs rewriting:** solver_probe HAS `--salary` at HEAD (:71) plus a Showdown-geometry refusal (:58-64). What survives: date-resolution still reads the shared staged copy with no draftgroup cross-check, and neither output names the salary file or draftgroup it read, so the filed wrong-draftgroup silence stands. No CHANGELOG entry mentions the flag (possible changelog-debt instance).
- **Line moves:** R172 :4202->:4221/:4231; R173 :1168->:1171-1172; R176(a) :1153->:1154-1155; R176(c) lives in late_swap_manager.py:377/:385 (not execution_pipeline); R179's seed guard is build_slate.py:46-50 (main entry :3324); R180 audit.py :1378->:1497, :1917->:2036.
- **Inbox census (the inbox is a directory, listed):** `2026-08-23_BUILD_ownership_pred_showdown_salary_dedupe.md` is LIVE, dropped by 8082d51 AFTER the 08-23 consume, and is the root cause under R194's second symptom; merge it as a rider on R194 rather than minting a number. The 08-09 curated-archetypes fragment is kept by design (Ben decision carrier).
- **Queue-sequencing risks found:** (1) R195 and R181 share one line, execution_pipeline.py:1325 `"wta_satellite": "large_wta"` -- land R195 FIRST or together, else R181's regex scoping masks R195's mapping defect behind a green eval. (2) R192 + R194 + the live dedupe fragment are one defect class (no shared CPT/UTIL-to-person resolution for Showdown DK files); the melt in showdown.py already solves it for the solver cap, and one helper serves all three surfaces. (3) R165 and R164(c) are the same normalization class two files apart; R55's shared normalizer closes both.

## 2.5 Landing-vs-claim audit (this week's landings)

R152: as claimed, except the fingerprint's own docstring claim (GF7-T1). R167: as claimed at all four sites, with the class's fifth member (GF7-T3) and the gate's NaN/bool hole (GF7-E2) open; the four resolvers agree on floor()+epsilon except showdown's (GF7-E5). R168(a): landed, property test real; (b): landed for `fetch_lineups`, two sibling doors remain (GF7-T4). R169: (a) landed for TimeoutExpired only (GF7-T2); (b)(c)(d) landed as claimed, with (d)'s replace-not-merge caveat (GF7-T5). R190(a): landed exactly as claimed (one /people call, both halves, both null-hand warnings). R159/R160: landed as claimed except the defer guard (GF7-E1) and the two edge findings (E6, E7). R153: landed as claimed except the floor rung (GF7-E3). R156: landed as claimed; note R153's cap-beats-lock precedence can still drop a duel arm into `player_cap_locks_dropped` with the thesis name unchanged. R154: landed as claimed, including its own "nothing calls attach on the production path yet"; note `_pct` now means fractions in one family and percentage points in the ownership pair, one suffix over two unit systems, deliberate but worth a name. R189(2): landed as claimed except the discarded collision report (GF7-E9). R136: landed as claimed (identity, tiers, captain rank); R171(a) untouched beside it.

MLB_Classic numerics vs code: checked F4 priors, both clips, shrink bounds, value-guard constants, ceiling multiplier formula and clips, K-rate TBF constant, and the section 8 posture table against `STRATEGY_DEFAULTS`: **no mismatches.** One prose staleness: section 6's "joins the Savant table directly, no name match" predates R189(2)'s name-join fallback.

## 2.6 Verified-clean census (so ed8 does not re-derive)

Gate machinery: class-coverage completeness, oversized-unit refusal by name, failing-unit propagation, torn-jsonl degradation, `gate_call_ceiling` floor, byte-identical clean line from one summarizer on both paths. autobuild: `assert_classification_in_sync` bidirectional; equals-form passthrough cannot outrank supervisor flags. build_slate: feed_note reaches the brief; AST tuple-return blind spots currently empty. Intake: `dk_side_readings` rejects duplicate/0/>9 slots; R151 normalizers diacritic-fold, so the R189(2) join does not reopen the accent class; a feed probable with id+hand is never overwritten by DK's nameless payload. Showdown: R153 player cap counts CPT+UTIL as one player (the ownership_pred trap does not recur in the solver); `_largest_remainder` and `_round_robin` deterministic under ties; hard_locks cannot self-deadlock with the captain cap; the `pct*n < 1` clamp consistent across resolvers. Ownership: `attach_projected_ownership` NaN-safe, tier untouched, unknown shape refused; `min_low_owned_hitters` slot-vocabulary-correct (P1/P2) and names its infeasibility. Allocator legacy half: no production caller (import census: pipeline takes `assert_fraction_cap` + `select_and_assign_entries`; bank_cache takes `ENTRY_ROSTER_SLOTS`; showdown takes `assert_fraction_cap`; late_swap takes vocabulary); no pool reduction anywhere in it (it selects among candidate lineups only). Preflight/verify: Showdown embedded-pool column collision impossible in both parsers (exact `"position"` cell match); preflight is stdlib-only with bounded globs, consistent with the <2s claim (not re-timed: outage). awaiting_standings beyond :200: solid (CPT/UTIL roster headers, two-signal filled rule, zero-byte handling, ET authority, `>=` settle guard). validate_dk_entries_file: every MLB_Classic section-10 check exists and reads the re-parsed exported bytes (conditional checks skip silently when caller context is absent, R177's known class). Tests: reuse family pins cap value, rungs, realized reuse, and refusals (strong); R153 cap tests pin realized counts biconditionally against the delivered artifact (strong); bank answered-job taxonomy pins skip/retry in both directions (moderate-strong with the X3 seam); golden replay still bypasses the front door (R83, known).

---

# Section 3: Target Architecture & Implementation Specs

Short by design, like ed6's: the standing target architecture is the adjudicated ed3 document plus the board's own sequencing, and six editions of evidence say re-arguing it produces nothing. What this edition adds is one re-pricing, two sequencing corrections, and the concrete specs for the zero-touch remainder.

## 3.1 The EV path is Tier 2, and R118 just got cheaper: the data is already retained

The board prices R118 (head of Tier 2) as retaining the per-player FPTS map the miner "computes and strips" and shipping the counterfactual replay tool. The strip point moved (:1961 -> field_miner.py:1982) and, decisively: **the strip removes only the DERIVED maps (`own_by_norm`, `fpts_by_norm`); `player_table` survives into every archived mined_*.json and already carries `{player, player_norm, roster_position, pct_drafted, fpts}` per row.** Verified against a real artifact pre-outage (data/archive/2026-06-03/mined_191020573.json: 52 rows, e.g. `{'player': 'Cristopher Sanchez', ..., 'fpts': 30.75}`). Consequence: **no miner change and no re-mine backfill is required.** The replay tool re-derives `fpts_by_norm` with the same three-line first-wins comprehension the miner uses (:704-709), from the archive as it sits.

Replay-tool spec (`tools/replay_contest.py`, ARCHIVE-adjacent read-only):
- Inputs: one archived `mined_*.json` (field entries with points/rank, player_table), the matching salary CSV (crosswalk), and a CANDIDATE construction policy expressed as lineups (player_norm sets, captain for Showdown).
- Score = sum of `fpts_by_norm` over the lineup (Showdown: verify the CPT-row convention, base vs 1.5x-applied, against ONE archived Showdown contest before grading any; `captain_norm` is archived per entry, so the lever exists either way).
- Self-validation bar, per the board: replaying every archived OWN entry must reproduce its recorded points and rank exactly; a lineup containing a player DK's table omits (the known multi-position deficit, `diagnostics.dk_table_deficit_pts`) is classified non-replayable-from-table, never approximated silently.
- Output: counterfactual finish positions of the candidate policy inside the REAL archived field, per contest, conditioned on archetype and field size, never pooled. Label: deterministic replay against observed outcomes. It grades construction policies; it is not a probability, a win rate, or an EV number, and this spec does not relabel it.
- Sequencing unchanged: R118 -> R48+R83 -> R10 -> R140, with R13 still the funding gate and the four sim-gate preconditions untouched.

## 3.2 Measurement-integrity precondition: land GF7-S1 before R10 grades duplication in any Showdown cell

R10's bar reads duplication from the miner. The miner's Showdown duplication is captain-blind at all three counting sites (GF7-S1), so the bar would read against inflated counts, and ledger 3.17's Showdown duplication rows need a one-sentence caveat until the recount lands. The recount is re-derivable from archived JSONs (captain_norm is already stored); no re-mine. This is one sentence on R10's entry now, one keyed fix riding R198's registry pass, and one ARCHIVE ledger caveat. Classic cells, including the unblocked satellite cell, are unaffected.

## 3.3 Zero-touch completion, specified

The remaining distance between the two clicks, with the fix shape for each (all S/XS unless noted):
1. **Lost windows (this edition):** GF7-T2 (one `_write` call), GF7-T4 (two guards, exit-4 payloads), GF7-T8 (coerce at the two boundaries), GF7-E2 (isfinite), GF7-T3 (sixth key through the gate). Each converts a mid-window crash or silent disable into a refusal or a named degradation.
2. **R121, unchanged:** `tools/slate_clock.py` (<2s, no engine import), a T-minus stderr line at build start, the SKILL same-turn-measurement rule. Still the only open item that changes what a session BELIEVES at decision time.
3. **R203 + R207 + GF7-T5, one supervisor batch (M):** (a) fold the operator's passthrough `--controls-override` into the supervisor's dict at startup (GF7-T5's fix is the precondition for everything else, else the supervisor discards the very rescue it is learning to perform); (b) detect the delegated rescue case off the allocator's own diffuse-infeasibility message (both structural remedies applied AND the error names no single binding control), run the sanity solve at 1.0 on all three caps, then set the delivered target from that slate's `player_exposure_floor`-family values in the feasibility report, never a constant; (c) R207's drop-one re-solve (five solves against the in-memory bank, emit a per-control binding table into the refusal) is the cheaper diagnostic that should land first and feed (b)'s detection. Record every step in `autobuild_decisions.json` under the existing schema (attempt, action, reason, controls_in_effect), which is also why GF7-T2 must land first: the log has to survive the window to be worth writing.
4. **The queue's slots 1-9 as re-verified**, in their standing order, with the two sequencing notes from 2.4 (R195 before R181; the Showdown person-resolution helper serving R192+R194+fragment).

Everything outside the two clicks stays refused by the walls. No watcher daemons (rejected; the scheduled task is the primitive), no DK polling, no auto-upload, no credential storage: unchanged, and re-affirmed here so the eighth edition can cite this one.

## 3.4 What earns EV language, unchanged

The bar is the one the record already holds: a calibrated joint outcome model, a contest-conditioned field, exact settlement, and uncertainty accounting, funded only through R13, technically gated on the variance basis and the separate-scenario-bank clauses. R154's ownership constraints exist in the optimizer and correctly remain uncalled on the production path until more graded slates accumulate; the first graded prediction (Spearman +0.581/+0.66, magnitude unusable) spends ordering on constraints and none of the magnitude on an objective, which is the right shape. Nothing in this edition moves that bar, and nothing in this repo today may be labeled EV, ROI, win rate, or a probability. The proprietary asset remains the archive: 271+ mined contests, own-entry ground truth, and the only calibration substrate no vendor publishes; GF7-S1 is the first defect found INSIDE that asset, which is exactly why it outranks its severity class.

## 3.5 Benchmark refresh

Searched 2026-08-24: nothing material has changed since ed6's sourced industry brief (its section 4 stands as current: vendor sim/field-model claims unaudited, the Haugh-Singal and Hunter-Vielma-Zaman frames, the open-source honesty benchmark, dupes ~ product-of-ownership). The only new hits are seasonal optimizer marketing and one already-cited LP paper (arXiv 2411.11012). Adopt ed6 section 4 by reference; no re-benchmark earned a slot this week.

---

# Method appendix

**Lanes:** (1) post-ed6 tools diff (audit gate machinery, qa_portfolio, autobuild, build_slate, fetch_slate_bundle, sync_check); (2) post-ed6 engine diff (live_data_adapters, showdown pair, ownership_prior, optimizer_v3, execution_pipeline, contest_allocator, projection_builder, paste_lineups, MLB_Classic numerics); (3) open-queue premise re-verification (slots 1-9 plus nine adjacent items, four repros, inbox census); (4) ed6's published cut list (allocator legacy half, field_miner non-intake half, awaiting_standings, slate_intake_manager, dk_entries_manager asserts, verify/promote/stage/manifest, _test_r3 litter); (5) test assertion strength (three "not assessed" families, R153 cap tests, SupervisorHardeningTests census, fixture hygiene). Coordinator re-read every High/Bug citation used in this document against the tree: audit.py:1016 + test_core exec sites, autobuild.py:189-241 and :208-219, contest_allocator.py:1035-1065, live_data_adapters.py:672-703 and :2258-2283, showdown_theses.py:668-725 kwarg census, field_miner.py:264/:770/:1244/:1343/:1982, build_slate.py:113-129, promote_run.py:160-176.

**Environment event, disclosed:** the Cowork Linux workspace bridge died mid-review ("Failed to create bridge sockets", permanent) after the early repros ran. Repros that completed before the outage and are quoted above: GF7-E1's degraded-side demo, R205's odds parse, R172's tier demo, R165's int64 demo, R181's two greps, agent 4's archived-artifact FPTS read. Everything after the outage is VERIFIED-read (file tools) or PLAUSIBLE-labeled. Two standing recipes were unavailable as a result: /tmp repro scripts did NOT survive for the landing session (the sandbox died with them; the reviews-race memory's keep-repros-in-/tmp advice assumes a living sandbox), and no end-of-session `git status` read-only proof could run. The write scope of this review is exactly two new files: this document and `docs/backlog_inbox/2026-08-24_REVIEW_greenfield-spec-ed7.md`. No existing file was modified, no claim taken, no engine path executed that writes, no git write command issued.

**Verification census:** 6 VERIFIED-repro (pre-outage, outputs quoted), 30 VERIFIED-read of which 13 coordinator re-read, 3 PLAUSIBLE (GF7-T8, GF7-E8 occurrence half, GF7-E5's concrete divergence value). Queue: 19 board items re-verified HOLDS, 1 PARTIALLY OVERTAKEN (R202), 0 WRONG, 0 OVERTAKEN.

**For the landing session:** renumber GF7 ids from the current board max at commit time (scan backlog AND changelog; R211 was the max at review time); expect none of the cited files to have moved if landing same-day, and re-verify GF7-E1/T1/T2/E3 at the landing head if not (the reviews-race rule). The highest-value hand-run mutations, per the tests lane: the reuse-ladder skip-intermediate-rungs mutation, the game-cap row deletion (GF7-X1), the value-guard percentile drop (GF7-X2), the bank membership loosening (GF7-X3), and the missing wall-clock test (GF7-T2). Suggested batch shape: GF7-T2+T4+T5 ride the lost-window family; E1+E6+E7 ride slot 1's intake surface; T3+E2+T8 extend R167's units family; E3+E4+E5 join the slot-9 Showdown batch; S1+S3 ride R198; T1 lands alone (gate surface, reset cost); X-series ride R91's harness item; H1's sweep is a Ben mv-grant away.
